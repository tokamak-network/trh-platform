---
phase: quick
plan: 260330-o60
subsystem: [tokamak-thanos, trh-sdk]
tags: [isthmus, eip-7702, bundler, aa, usdc]
dependency_graph:
  requires: [260330-nev]
  provides: [isthmus-hardfork, alto-bundler, eip-7702-delegation]
  affects: [trh-sdk/stacks/thanos, tokamak-thanos/op-chain-ops]
tech_stack:
  added: [holiman/uint256, ghcr.io/pimlicolabs/alto:v1.2.7]
  patterns: [EIP-7702 SetCodeTx, LatestSignerForChainID, self-delegation]
key_files:
  created: []
  modified:
    - /Users/theo/workspace_tokamak/tokamak-thanos/op-chain-ops/genesis/config.go
    - /Users/theo/workspace_tokamak/trh-sdk/pkg/types/deploy_config_template.go
    - /Users/theo/workspace_tokamak/trh-sdk/pkg/stacks/thanos/input.go
    - /Users/theo/workspace_tokamak/trh-sdk/pkg/stacks/thanos/templates/local-compose.yml.tmpl
    - /Users/theo/workspace_tokamak/trh-sdk/pkg/stacks/thanos/aa_setup.go
decisions:
  - "D-01: L2GenesisIsthmusTimeOffset=0x0 (genesis 즉시 Isthmus 활성화)"
  - "D-02: Alto bundler EntryPoint v0.8 predeploy 0x420...0063 사용"
  - "D-03: EIP-7702 delegation은 setupAAPaymaster() 마지막 단계"
  - "D-04: types.SetCodeTx + LatestSignerForChainID (EIP155Signer 사용 금지)"
metrics:
  duration: 15min
  completed_date: "2026-03-30"
  tasks: 3
  files: 5
---

# Quick Task 260330-o60: EIP-7702 Bundler Implementation for USDC Summary

EIP-7702 기반 USDC fee token AA 인프라 완성을 위한 3단계 구현: Isthmus hardfork 활성화, Alto bundler 컨테이너 추가, EIP-7702 delegation 자동화.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | tokamak-thanos Isthmus hardfork 활성화 | tokamak-thanos@4efcb38, trh-sdk@e4e7d9a | config.go, deploy_config_template.go, input.go |
| 2 | Alto bundler를 docker-compose 템플릿에 추가 | trh-sdk@4b3bb32 | local-compose.yml.tmpl |
| 3 | setupAAPaymaster()에 EIP-7702 delegation 추가 | trh-sdk@de8cbfc | aa_setup.go |

## What Was Built

### Task 1: Isthmus Hardfork 활성화 파이프라인

**tokamak-thanos/op-chain-ops/genesis/config.go:**
- `L2GenesisIsthmusTimeOffset *hexutil.Uint64` 필드를 DeployConfig 구조체에 추가 (L2GenesisInteropTimeOffset 아래)
- `IsthmusTime(genesisTime uint64) *uint64` 함수 추가 (FjordTime() 패턴 동일)
- `RollupConfig()` 초기화 블록에 `IsthmusTime: d.IsthmusTime(l1StartBlock.Time())` 연결

**trh-sdk/pkg/types/deploy_config_template.go:**
- `L2GenesisIsthmusTimeOffset string` 필드 추가 (L2GenesisFjordTimeOffset 아래)

**trh-sdk/pkg/stacks/thanos/input.go:**
- `L2GenesisIsthmusTimeOffset: "0x0"` 기본값 설정 (genesis부터 즉시 활성화)

### Task 2: Alto Bundler 서비스 추가

**trh-sdk/pkg/stacks/thanos/templates/local-compose.yml.tmpl:**
- `{{- if .AAOperatorEnabled }}` 조건부 블록 내 aa-operator 서비스 아래에 alto-bundler 추가
- `ghcr.io/pimlicolabs/alto:v1.2.7` 이미지 사용
- EntryPoint v0.8 predeploy: `0x4200000000000000000000000000000000000063`
- `--chain-type op-stack`, `--safe-mode false` (로컬 devnet 설정)
- 포트 4337:4337 (ERC-4337 관례 포트)

### Task 3: EIP-7702 Delegation 자동화

**trh-sdk/pkg/stacks/thanos/aa_setup.go:**
- `holiman/uint256` import 추가
- Step 5: EIP-7702 delegation 코드 추가 (verifyAAPaymaster 호출 이후)
  - `simple7702Addr = 0x4200000000000000000000000000000000000065`
  - `types.SetCodeAuthorization` + `types.SignSetCode(privKey, auth)` 서명
  - `types.SetCodeTx` + `types.LatestSignerForChainID(l2ChainID)` 트랜잭션 전송
  - 30회 폴링으로 receipt 확인 (60초 타임아웃)

## Deviations from Plan

None - 플랜이 정확히 의도한 대로 구현되었습니다.

## Build Verification

- tokamak-thanos genesis 및 op-node 패키지 빌드 성공
- trh-sdk 전체 빌드 성공 (`go build ./...`)
- 기존 오류(check-ecotone, unclaimed-credits)는 이번 변경과 무관한 사전 존재 오류

## Known Stubs

없음. 모든 구현이 실제 동작 코드이며 placeholder 없음.

## Self-Check: PASSED

- config.go: L2GenesisIsthmusTimeOffset 필드, IsthmusTime() 함수, RollupConfig 세팅 - 확인
- deploy_config_template.go: L2GenesisIsthmusTimeOffset 필드 - 확인
- input.go: L2GenesisIsthmusTimeOffset: "0x0" - 확인
- local-compose.yml.tmpl: alto-bundler 서비스 블록 - 확인
- aa_setup.go: SetCodeTx, SignSetCode, LatestSignerForChainID - 확인
- tokamak-thanos@4efcb38, trh-sdk@e4e7d9a/4b3bb32/de8cbfc - 모든 커밋 존재 확인
