---
phase: quick
plan: 260330-rx9
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/theo/workspace_tokamak/tokamak-thanos/packages/tokamak/sdk/src/paymaster-signer.ts
autonomous: true
requirements:
  - fix-paymasterAndData-format
  - fix-userOp-signature

must_haves:
  truths:
    - "paymasterAndData는 52 bytes 이상으로 EntryPoint require(length >= 52) 통과"
    - "MultiTokenPaymaster가 [20:40]에서 token address를 정상으로 읽음"
    - "UserOp 서명이 raw ECDSA로 Simple7702Account.ECDSA.recover(hash, sig) 검증 통과"
    - "npx tsc --noEmit가 에러 없이 통과"
  artifacts:
    - path: "packages/tokamak/sdk/src/paymaster-signer.ts"
      provides: "두 버그 수정이 적용된 paymaster-signer 모듈"
      contains: "PAYMASTER_PADDING"
  key_links:
    - from: "buildPaymasterAndData"
      to: "MULTI_TOKEN_PAYMASTER + tokenAddr + PAYMASTER_PADDING"
      via: "ethers.utils.hexConcat"
      pattern: "hexConcat.*PAYMASTER_PADDING"
    - from: "signUserOpHash"
      to: "eth_sign RPC or _signingKey().signDigest"
      via: "signer type check"
      pattern: "_signingKey.*signDigest|eth_sign"
---

<objective>
paymaster-signer.ts의 두 가지 버그를 원자적으로 수정한다.

Purpose: 현재 구현은 EntryPoint의 paymasterAndData 최소 길이(52 bytes) 검증과 Simple7702Account의 raw ECDSA 서명 검증에서 모두 실패한다. 이 두 버그를 수정하면 UserOperation이 EntryPoint 검증을 통과하고 계정 서명 검증을 통과한다.

Output: 수정된 paymaster-signer.ts — buildPaymasterAndData(52 bytes), signUserOpHash 헬퍼(raw ECDSA)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/theo/workspace_tokamak/tokamak-thanos/packages/tokamak/sdk/src/paymaster-signer.ts

<interfaces>
<!-- 수정 대상 위치 (line 번호 기준) -->

Fix 1 — buildPaymasterAndData (line 65-66):
현재:
  const buildPaymasterAndData = (tokenAddr: string): string =>
    ethers.utils.hexConcat([MULTI_TOKEN_PAYMASTER, tokenAddr])
  // 결과: 40 bytes → EntryPoint "AA93 invalid paymasterAndData" revert

수정 후:
  const PAYMASTER_PADDING = '0x000000000000000000000000' // 12 bytes zero → total 52 bytes
  const buildPaymasterAndData = (tokenAddr: string): string =>
    ethers.utils.hexConcat([MULTI_TOKEN_PAYMASTER, tokenAddr, PAYMASTER_PADDING])
  // [0:20] = paymaster, [20:40] = tokenAddr (MultiTokenPaymaster reads here), [40:52] = padding

Fix 2 — 서명 헬퍼 추가 + 호출처 교체 (line 299, 361):
현재 (두 곳):
  await signer.signMessage(ethers.utils.arrayify(userOpHash))  // personal_sign → prefix 붙음

수정 후 — 헬퍼 함수 추가:
  const signUserOpHash = async (signer: ethers.Signer, userOpHash: string): Promise<string> => {
    const sender = await signer.getAddress()
    // ethers.Wallet: _signingKey() 있음 → raw ECDSA 직접 가능
    if ('_signingKey' in signer && typeof (signer as any)._signingKey === 'function') {
      const wallet = signer as ethers.Wallet
      const sig = wallet._signingKey().signDigest(ethers.utils.arrayify(userOpHash))
      return ethers.utils.joinSignature(sig)
    }
    // JsonRpcSigner (wagmi Web3Provider): eth_sign = raw ECDSA (MetaMask 기준, no prefix)
    return (signer.provider as ethers.providers.JsonRpcProvider).send('eth_sign', [sender, userOpHash])
  }

호출처 교체:
  line 299: const approveSignature = await signUserOpHash(signer, approveUserOpHash)
  line 361: const signature = await signUserOpHash(signer, userOpHash)

왜 eth_sign인가:
  - Simple7702Account: ECDSA.recover(hash, signature) == address(this) — prefix 없는 raw hash
  - signer.signMessage()는 personal_sign RPC → "\x19Ethereum Signed Message:\n32" prefix 추가 → recover가 다른 주소 반환 → SIG_VALIDATION_FAILED
  - eth_sign(MetaMask/wagmi): prefix 없는 raw ECDSA 반환
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: paymasterAndData format fix (40→52 bytes) + raw ECDSA signature helper</name>
  <files>/Users/theo/workspace_tokamak/tokamak-thanos/packages/tokamak/sdk/src/paymaster-signer.ts</files>
  <action>
다음 세 가지 변경을 paymaster-signer.ts에 적용한다. 다른 코드는 일절 수정하지 않는다.

**변경 1: PAYMASTER_PADDING 상수 추가 (line 65 앞)**

buildPaymasterAndData 함수 바로 위에 상수를 추가한다:
```typescript
// 12 bytes zero padding → total 52 bytes (EntryPoint PAYMASTER_DATA_OFFSET minimum)
const PAYMASTER_PADDING = '0x000000000000000000000000'
```

**변경 2: buildPaymasterAndData 함수 수정 (line 65-66)**

기존:
```typescript
const buildPaymasterAndData = (tokenAddr: string): string =>
  ethers.utils.hexConcat([MULTI_TOKEN_PAYMASTER, tokenAddr])
```

수정:
```typescript
const buildPaymasterAndData = (tokenAddr: string): string =>
  ethers.utils.hexConcat([MULTI_TOKEN_PAYMASTER, tokenAddr, PAYMASTER_PADDING])
```

**변경 3: signUserOpHash 헬퍼 추가 (buildUserOpHash 함수 바로 뒤, sendRawUserOp 앞)**

```typescript
/**
 * Signs userOpHash with raw ECDSA (no EIP-191 prefix).
 * Simple7702Account expects: ECDSA.recover(hash, signature) == address(this)
 *
 * - ethers.Wallet: uses _signingKey().signDigest() for guaranteed raw ECDSA
 * - JsonRpcSigner (wagmi Web3Provider): uses eth_sign RPC
 *   MetaMask eth_sign returns raw ECDSA without prefix (unlike personal_sign)
 */
const signUserOpHash = async (
  signer: ethers.Signer,
  userOpHash: string
): Promise<string> => {
  const sender = await signer.getAddress()

  // ethers.Wallet path: raw ECDSA directly via _signingKey
  if ('_signingKey' in signer && typeof (signer as any)._signingKey === 'function') {
    const wallet = signer as ethers.Wallet
    const sig = wallet._signingKey().signDigest(ethers.utils.arrayify(userOpHash))
    return ethers.utils.joinSignature(sig)
  }

  // JsonRpcSigner path: eth_sign (raw ECDSA in MetaMask, no prefix)
  return (signer.provider as ethers.providers.JsonRpcProvider).send('eth_sign', [
    sender,
    userOpHash,
  ])
}
```

**변경 4: signMessage 호출 교체 (두 곳)**

line 299:
기존: `const approveSignature = await signer.signMessage(ethers.utils.arrayify(approveUserOpHash))`
수정: `const approveSignature = await signUserOpHash(signer, approveUserOpHash)`

line 361:
기존: `const signature = await signer.signMessage(ethers.utils.arrayify(userOpHash))`
수정: `const signature = await signUserOpHash(signer, userOpHash)`

**변경 5: line 342 주석 업데이트**

기존: `// 8. paymasterAndData (Phase 1: 40 bytes)`
수정: `// 8. paymasterAndData (52 bytes: paymaster(20) + token(20) + padding(12))`
  </action>
  <verify>
    <automated>cd /Users/theo/workspace_tokamak/tokamak-thanos/packages/tokamak/sdk && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - PAYMASTER_PADDING 상수가 파일에 존재하고 '0x000000000000000000000000' (12 bytes)
    - buildPaymasterAndData가 hexConcat 3개 인자 (paymaster, token, padding)
    - signUserOpHash 헬퍼 함수가 파일에 존재
    - signMessage 호출이 두 곳 모두 signUserOpHash로 교체됨
    - npx tsc --noEmit 에러 없음
  </done>
</task>

</tasks>

<verification>
수정 완료 후 검증:

1. paymasterAndData 길이 확인:
   - PAYMASTER_PADDING = '0x000000000000000000000000' (12 bytes = 24 hex chars)
   - 총 길이: 20(paymaster) + 20(token) + 12(padding) = 52 bytes
   - EntryPoint require(paymasterAndData.length >= 52) 통과

2. token offset 확인:
   - bytes[20:40] = tokenAddr → MultiTokenPaymaster.paymasterAndData[20:40] 정상 파싱

3. 서명 방식 확인:
   - signUserOpHash: _signingKey 분기(Wallet) 또는 eth_sign RPC(JsonRpcSigner)
   - 두 경로 모두 raw ECDSA (no prefix)
   - Simple7702Account.ECDSA.recover(hash, sig) == sender 성립

4. TypeScript 컴파일:
   npx tsc --noEmit (tokamak-thanos/packages/tokamak/sdk 디렉토리)
</verification>

<success_criteria>
- buildPaymasterAndData가 52 bytes hex string을 반환 (PAYMASTER_PADDING 포함)
- signUserOpHash 헬퍼가 ethers.Wallet 및 JsonRpcSigner 두 경로를 모두 처리
- signer.signMessage 호출이 paymaster-signer.ts 내에 존재하지 않음
- npx tsc --noEmit이 에러 없이 통과
</success_criteria>

<output>
완료 후 다음 내용으로 SUMMARY를 작성하지 않아도 됨 (quick task).
커밋 메시지: `fix(sdk): fix paymasterAndData to 52 bytes and use raw ECDSA signature`
커밋 대상: `/Users/theo/workspace_tokamak/tokamak-thanos/packages/tokamak/sdk/src/paymaster-signer.ts`
</output>
