---
phase: quick
plan: 260401-mn3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/index.ts
  - src/main/preload.ts
  - src/renderer/App.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Electron app을 재시작해도 Docker 컨테이너는 계속 실행 중이다"
    - "재시작 후 앱이 뜰 때 컨테이너가 이미 실행 중이면 setup을 건너뛰고 webapp으로 바로 이동한다"
    - "Tray 메뉴와 webapp gear 메뉴에 'Restart App' 항목이 있다"
  artifacts:
    - path: "src/main/index.ts"
      provides: "isRelaunching 플래그 + app:relaunch IPC handler + Tray Restart App 메뉴"
    - path: "src/main/preload.ts"
      provides: "app.relaunch() API 노출"
    - path: "src/renderer/App.tsx"
      provides: "gear 메뉴에 Restart App 버튼"
  key_links:
    - from: "src/renderer/App.tsx gear menu"
      to: "src/main/index.ts app:relaunch handler"
      via: "ipcRenderer.invoke('app:relaunch')"
    - from: "src/main/index.ts before-quit"
      to: "stopContainers()"
      via: "isRelaunching 플래그로 skip"
---

<objective>
컨테이너를 유지한 채로 Electron app만 재시작하는 기능 구현.

Purpose: 개발/운영 중 컨테이너는 살리면서 앱 프로세스만 재시작하고 싶을 때 (UI 버그 복구, 설정 변경 등).
Output: Tray + webapp gear 메뉴에 "Restart App" 항목, 재시작 후 컨테이너 유지 + setup 자동 skip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md

<!-- Key interfaces for executor -->
<interfaces>
<!-- src/main/index.ts 현재 관련 변수/패턴 -->
```typescript
let isQuitting = false;  // 현재 존재하는 플래그 — isRelaunching 추가 필요

// before-quit 핸들러 (708~725라인):
app.on('before-quit', async (event) => {
  if (isQuitting) return;
  isQuitting = true;
  event.preventDefault();
  destroyPlatformView();
  cleanupProcesses();
  try {
    await stopContainers();  // isRelaunching=true 일 때 이 줄을 skip해야 함
  } catch (error) { ... }
  app.exit(0);
});

// Tray buildTrayMenu() 함수에 이미 "Restart Services" 항목 있음 (228~258라인)
// 그 뒤에 "Restart App" 항목을 추가하면 됨
```

<!-- src/main/preload.ts 현재 app 객체 -->
```typescript
app: {
  loadPlatform: ...,
  openExternal: ...,
  getVersion: ...,
  onMenuUninstall: ...,
  // relaunch: 추가 필요
}
```

<!-- src/renderer/App.tsx 현재 gear 메뉴 구조 -->
```typescript
// viewMode === 'webapp' 케이스에서 gear dropdown:
<div className="gear-dropdown">
  {version && <div className="gear-version">TRH Platform v{version}</div>}
  <button className="gear-uninstall-btn" onClick={...}>
    Uninstall
  </button>
  {/* Restart App 버튼 여기에 추가 */}
</div>

// App.tsx useEffect에 이미 있는 자동 skip 로직 (lines 30-37):
const status = await api.docker.getStatus();
if (status.healthy) {
  await api.app.loadPlatform({ adminEmail: ADMIN_EMAIL, adminPassword: ADMIN_PASSWORD });
  setViewMode('webapp');
}
// → 재시작 후에도 이 로직이 동작하여 setup 자동 skip됨 (이미 구현됨)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Main process — isRelaunching 플래그 + IPC handler + Tray 메뉴</name>
  <files>src/main/index.ts</files>
  <action>
    3가지 변경:

    **1) isRelaunching 플래그 추가**
    기존 `let isQuitting = false;` 아래에 추가:
    ```typescript
    let isRelaunching = false;
    ```

    **2) before-quit 핸들러 수정**
    `stopContainers()` 호출을 `isRelaunching` 플래그로 감싸서 skip:
    ```typescript
    app.on('before-quit', async (event) => {
      if (isQuitting) return;
      isQuitting = true;
      // ...
      destroyPlatformView();
      cleanupProcesses();
      if (!isRelaunching) {
        try {
          await stopContainers();
        } catch (error) {
          console.error('Failed to stop containers on quit:', error);
        }
      }
      app.exit(0);
    });
    ```

    **3) setupIpcHandlers()에 app:relaunch 핸들러 추가**
    `ipcMain.handle('app:get-version', ...)` 아래에 추가:
    ```typescript
    ipcMain.handle('app:relaunch', () => {
      isRelaunching = true;
      app.relaunch();
      app.quit();
    });
    ```

    **4) buildTrayMenu()에 "Restart App" 항목 추가**
    "Restart Services" 항목 바로 뒤(Stop Services 앞)에 추가:
    ```typescript
    {
      label: 'Restart App',
      click: () => {
        isRelaunching = true;
        app.relaunch();
        app.quit();
      }
    },
    ```
  </action>
  <verify>TypeScript 컴파일 통과: npx tsc --project tsconfig.electron.json --noEmit</verify>
  <done>컴파일 에러 없음. isRelaunching 플래그가 before-quit에서 stopContainers를 skip함.</done>
</task>

<task type="auto">
  <name>Task 2: Preload API 노출 + webapp gear 메뉴에 Restart App 버튼</name>
  <files>src/main/preload.ts, src/renderer/App.tsx</files>
  <action>
    **src/main/preload.ts 수정**
    `app` 객체에 `relaunch` 메서드 추가 (`getVersion` 바로 아래):
    ```typescript
    relaunch: (): Promise<void> => ipcRenderer.invoke('app:relaunch'),
    ```

    **src/renderer/App.tsx 수정**
    gear dropdown 안에서 기존 Uninstall 버튼 위에 "Restart App" 버튼 추가:
    ```typescript
    <button
      className="gear-restart-btn"
      onClick={() => {
        setGearOpen(false);
        api.app.relaunch();
      }}
    >
      Restart App
    </button>
    ```

    버튼 스타일은 기존 `gear-uninstall-btn`과 동일한 패턴으로 SetupPage.css 또는 App.tsx에 inline으로 적용해도 무방 (CSS 파일이 없으면 gear-uninstall-btn 클래스를 그대로 사용).
    실제로 App.tsx에 별도 CSS import가 없으므로 `className="gear-uninstall-btn"` 재사용하거나 새 클래스를 추가할 필요 없이 기존 스타일 활용.
  </action>
  <verify>TypeScript 컴파일 통과: npx tsc --project tsconfig.renderer.json --noEmit</verify>
  <done>
    - window.electronAPI.app.relaunch()가 타입 에러 없이 호출 가능
    - webapp 뷰의 gear 드롭다운에 "Restart App" 버튼이 렌더링됨
    - 재시작 후 컨테이너가 healthy 상태면 setup 없이 webapp으로 바로 이동 (App.tsx useEffect에 이미 구현됨)
  </done>
</task>

</tasks>

<verification>
수동 확인 절차:
1. `npm run build` 빌드 후 앱 실행
2. Docker 컨테이너 시작 완료 후 webapp 뷰 확인
3. Tray 메뉴 → "Restart App" 클릭 — 앱만 재시작, `docker ps`로 컨테이너 살아있음 확인
4. Gear 메뉴 → "Restart App" 클릭 — 동일 동작 확인
5. 재시작 후 앱이 setup 없이 webapp으로 바로 이동하는지 확인
</verification>

<success_criteria>
- `docker ps` 출력에서 컨테이너가 재시작 전후 동일하게 유지됨
- 앱 재시작 후 SetupPage 없이 webapp이 바로 표시됨
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
작업 완료 후 `.planning/quick/260401-mn3-electron-app/260401-mn3-SUMMARY.md` 생성.
STATE.md Quick Tasks Completed 테이블에 이 태스크 추가.
</output>
