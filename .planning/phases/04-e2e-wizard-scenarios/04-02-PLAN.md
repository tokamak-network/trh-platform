---
phase: 04-e2e-wizard-scenarios
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - tests/e2e/preset-wizard.spec.ts
autonomous: false
requirements:
  - E2E-01
  - E2E-02
  - E2E-03
  - E2E-04

must_haves:
  truths:
    - "4개 Preset 각각에 대해 3-step wizard 흐름(선택 -> 기본정보 -> 검토)을 Playwright가 자동 완주한다"
    - "각 Preset 선택 시 올바른 모듈 미리보기와 preset 정보가 검토 화면에 표시된다"
    - "잔액 미달 시 funding status에 Pending 표시, 잔액 충분 시 All funded 표시"
    - "배포 시작 후 deploymentId가 반환되고 페이지가 /rollup으로 이동한다"
  artifacts:
    - path: "tests/e2e/preset-wizard.spec.ts"
      provides: "4-preset parametric E2E wizard tests covering E2E-01~04"
      min_lines: 150
  key_links:
    - from: "tests/e2e/preset-wizard.spec.ts"
      to: "http://localhost:3000/rollup/create"
      via: "page.goto"
      pattern: "page\\.goto.*rollup/create"
    - from: "tests/e2e/preset-wizard.spec.ts"
      to: "tests/e2e/helpers/auth.ts"
      via: "authenticateContext import"
      pattern: "import.*authenticateContext"
    - from: "tests/e2e/preset-wizard.spec.ts"
      to: "../trh-platform-ui/src/mocks/handlers.ts"
      via: "MSW intercepts API calls during test execution"
      pattern: "MSW (implicit)"
---

<objective>
4개 Preset에 대한 E2E wizard 흐름 시나리오를 Playwright로 구현한다 (E2E-01~04).

Purpose: Preset 선택부터 배포 시작까지 전체 사용자 흐름이 브라우저에서 정상 동작하는지 자동 검증.
Output: tests/e2e/preset-wizard.spec.ts (파라메트릭 4-preset E2E 테스트)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-e2e-wizard-scenarios/04-CONTEXT.md
@.planning/phases/04-e2e-wizard-scenarios/04-RESEARCH.md
@.planning/phases/04-e2e-wizard-scenarios/04-01-SUMMARY.md

@playwright.config.ts
@tests/e2e/helpers/auth.ts
@../trh-platform-ui/src/mocks/handlers.ts

<interfaces>
<!-- Auth helper from Plan 01 -->
From tests/e2e/helpers/auth.ts:
```typescript
export async function authenticateContext(context: BrowserContext): Promise<void>;
// Sets auth-token cookie + localStorage.accessToken
```

<!-- Wizard UI selectors from RESEARCH.md -->
Step 1 (PresetSelectionStep):
- Heading: page.getByText('Choose a Deployment Preset')
- Preset cards: page.getByText('General Purpose'), page.getByText('DeFi'), page.getByText('Gaming'), page.getByText('Full Suite')
- Confirmation: page.getByText('Preset selected')
- Next: page.getByRole('button', { name: 'Next' })

Step 2 (BasicInfoStep):
- Infra provider: page.getByText('Local Docker')
- Chain Name: page.locator('#chainName')
- L1 RPC URL: page.locator('#l1RpcUrl')
- Network select: Radix UI combobox (trigger click -> option click)
- Next: page.getByRole('button', { name: 'Next' })

Step 3 (ConfigReview):
- Preset badge: page.getByText('{name} Preset')
- Deploy: page.getByRole('button', { name: 'Deploy Rollup' })

<!-- Funding status (E2E-03) from RESEARCH.md -->
Deploy flow: click "Deploy Rollup" -> POST /preset-deploy -> deploymentId -> router.push('/rollup')
Funding page: /rollup shows FundingStatus with allFulfilled check
- unfunded: "Pending" badge, "0 of N accounts funded"
- funded: "All accounts are funded"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create parametric 4-preset E2E wizard spec (E2E-01, E2E-02)</name>
  <files>tests/e2e/preset-wizard.spec.ts</files>
  <read_first>
    - /Users/theo/workspace_tokamak/trh-platform/.planning/phases/04-e2e-wizard-scenarios/04-RESEARCH.md (Wizard UI Structure Analysis section, Step 1/2/3 selectors, Radix UI pitfall, Seed phrase input question)
    - /Users/theo/workspace_tokamak/trh-platform/tests/e2e/helpers/auth.ts
    - /Users/theo/workspace_tokamak/trh-platform-ui/src/mocks/handlers.ts
    - /Users/theo/workspace_tokamak/trh-platform-ui/src/features/rollup/components/wizard/steps/BasicInfoStep.tsx (seed phrase input DOM structure)
    - /Users/theo/workspace_tokamak/trh-platform-ui/src/features/rollup/components/wizard/steps/PresetSelectionStep.tsx (preset card selectors)
    - /Users/theo/workspace_tokamak/trh-platform-ui/src/features/rollup/components/wizard/steps/ConfigReview.tsx (review page selectors, parameter display)
    - /Users/theo/workspace_tokamak/trh-platform-ui/src/features/rollup/components/wizard/WizardFooter.tsx (navigation button selectors)
    - /Users/theo/workspace_tokamak/trh-platform-ui/src/features/rollup/components/account/AccountSetup.tsx (seed phrase input structure for mode="preset")
  </read_first>
  <action>
Create `tests/e2e/preset-wizard.spec.ts` implementing E2E-01 and E2E-02 (per D-08 parametric pattern):

1. Import `test, expect` from `@playwright/test` and `authenticateContext` from `./helpers/auth`.

2. Define preset mapping constant:
```typescript
const PRESETS = [
  { id: 'general', name: 'General Purpose' },
  { id: 'defi', name: 'DeFi' },
  { id: 'gaming', name: 'Gaming' },
  { id: 'full', name: 'Full Suite' },
] as const;
```
NOTE: Exact preset card text labels must be verified by reading PresetSelectionStep.tsx and MOCK_PRESETS. Adjust names to match actual UI text.

3. `test.beforeEach`: call `authenticateContext(context)` to inject auth cookie and localStorage token.

4. For each preset, create `test.describe` block with:

**Test: E2E-01 "completes 3-step wizard flow":**
- `page.goto('/rollup/create')`
- Wait for preset cards: `await expect(page.getByText('Choose a Deployment Preset')).toBeVisible()`
- Step 1: Click preset card by text name, verify "Preset selected" text appears, click Next button
- Step 2: Click "Local Docker" for infra provider, fill `#chainName` with `test-chain-{preset}`, fill `#l1RpcUrl` with `https://eth-sepolia.example.com`
  - For seed phrase: Read AccountSetup.tsx to discover input DOM structure. Fill 12 words of a valid BIP39 mnemonic (use: "abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about")
  - For Radix UI select (Network): click trigger element, then click "Testnet" option using `getByRole('option', { name: 'Testnet' })`
  - Click Next button
- Step 3: Verify review screen is visible. Check that preset name badge is shown (e.g., "General Purpose Preset" text).
- Assert: all 3 steps completed without error

**Test: E2E-02 "shows correct module preview and parameters":**
- Navigate through step 1 and 2 (same flow as E2E-01)
- On step 3 (ConfigReview): verify that chain parameter values are displayed (check for specific text content like parameter labels and values from MOCK_PRESETS/MOCK_PRESET_DETAILS)
- The exact values to check depend on what ConfigReview renders. Read ConfigReview.tsx to identify which fields are displayed (e.g., batchFrequency, outputFrequency, challengePeriod).
- Assert: preset-specific parameters are visible on review page

CRITICAL SELECTORS (from research, verify against actual source):
- NO data-testid attributes exist. Use role/text/label/id selectors only.
- Radix UI Select: click trigger -> click option (NOT native selectOption)
- Do NOT use `page.waitForTimeout()` — use `expect(locator).toBeVisible()` or `page.waitForSelector()`
- After `page.fill()`, consider blur event or wait for validation before clicking Next (Research Pitfall 4)
  </action>
  <verify>
    <automated>cd /Users/theo/workspace_tokamak/trh-platform && npx playwright test tests/e2e/preset-wizard.spec.ts --grep "wizard flow|module preview" --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/preset-wizard.spec.ts` contains `for (const preset of PRESETS)` or equivalent parametric loop
    - File contains exactly 4 preset entries in the PRESETS array
    - File imports `authenticateContext` from `./helpers/auth`
    - File contains `test.beforeEach` that calls `authenticateContext`
    - E2E-01 test navigates through all 3 steps: `page.goto('/rollup/create')`, step 1 preset selection, step 2 basic info, step 3 review
    - E2E-01 test clicks "Next" button twice (step 1->2, step 2->3)
    - E2E-02 test verifies preset-specific content on step 3 review screen
    - No `page.waitForTimeout()` calls exist in the file
    - All 4 presets x 2 tests = 8 test cases pass (or document specific failures with root cause)
  </acceptance_criteria>
  <done>4-preset parametric E2E tests for wizard 3-step flow (E2E-01) and module/parameter preview (E2E-02) pass in Playwright</done>
</task>

<task type="auto">
  <name>Task 2: Add funding status and deploy progress E2E scenarios (E2E-03, E2E-04)</name>
  <files>tests/e2e/preset-wizard.spec.ts, ../trh-platform-ui/src/mocks/handlers.ts</files>
  <read_first>
    - /Users/theo/workspace_tokamak/trh-platform/tests/e2e/preset-wizard.spec.ts (Task 1 output)
    - /Users/theo/workspace_tokamak/trh-platform-ui/src/mocks/handlers.ts (current handlers)
    - /Users/theo/workspace_tokamak/trh-platform/.planning/phases/04-e2e-wizard-scenarios/04-RESEARCH.md (Funding Status Scenarios section, E2E-04 section)
    - /Users/theo/workspace_tokamak/trh-platform-ui/src/features/rollup/components/funding/FundingStatus.tsx (funding UI structure, text content)
    - /Users/theo/workspace_tokamak/trh-platform-ui/src/app/rollup/page.tsx (post-deploy /rollup page structure)
  </read_first>
  <action>
Add E2E-03 and E2E-04 test scenarios to `tests/e2e/preset-wizard.spec.ts`:

**E2E-03: Funding status verification (per D-09)**

Per research: Deploy button on step 3 is always enabled. The funding check happens AFTER deploy on the /rollup page. Two scenarios needed:

Scenario A — "unfunded":
- Complete wizard steps 1-3 for one preset (e.g., 'general')
- Click "Deploy Rollup" on step 3
- MSW returns `{ deploymentId: 'test-deploy-001' }` for POST /preset-deploy
- Page redirects to `/rollup`
- MSW funding handler returns `allFulfilled: false`
- Verify: FundingStatus shows unfunded state text (read FundingStatus.tsx to determine exact text — expect something like "Pending" or "0 of N funded")

Scenario B — "funded":
- Use `page.route()` to override MSW funding response with `allFulfilled: true` for this test only (Playwright route takes priority over MSW)
  OR modify MSW handler via page.evaluate if the handler exports a scenario toggle
- Complete wizard, click deploy
- Verify: FundingStatus shows funded state text (e.g., "All accounts are funded")

**E2E-04: Deploy initiation and progress (per D-10)**
- Complete wizard steps 1-3
- Click "Deploy Rollup"
- Verify POST /preset-deploy was called (intercept with `page.waitForResponse('**/preset-deploy')`)
- Verify page navigates to `/rollup` (check `page.url()` contains `/rollup`)
- Verify deployment appears in the page (toast message "Deployment initiated!" or deployment list entry)

NOTE: The exact text and UI elements for funding status and deploy progress MUST be determined by reading the actual source files (FundingStatus.tsx, /rollup/page.tsx). Research has open questions about these. The executor must read those files before writing assertions.

For funding scenario switching, prefer `page.route()` over modifying MSW handlers — it's per-test, doesn't require MSW handler changes, and Playwright route interception happens before MSW:
```typescript
await page.route('**/preset-deploy/*/funding', (route) => {
  route.fulfill({
    contentType: 'application/json',
    body: JSON.stringify({ data: { allFulfilled: true, accounts: [...], status: 'ready' }, success: true }),
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/theo/workspace_tokamak/trh-platform && npx playwright test tests/e2e/preset-wizard.spec.ts --grep "funding|deploy" --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/preset-wizard.spec.ts` contains test with "funding" or "unfunded" in description
    - File contains test with "funded" or "all funded" scenario
    - E2E-03 unfunded test verifies text indicating insufficient funds on /rollup page
    - E2E-03 funded test uses `page.route()` to override funding API response
    - E2E-04 test uses `page.waitForResponse` to verify POST /preset-deploy was called
    - E2E-04 test asserts `page.url()` contains `/rollup` after deploy
    - All E2E-03 and E2E-04 tests pass (or document specific failures with root cause)
  </acceptance_criteria>
  <done>Funding status (unfunded/funded) and deploy progress E2E scenarios pass, covering E2E-03 and E2E-04 requirements</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full E2E test suite</name>
  <what-built>Complete E2E test suite: 4-preset parametric wizard flow (E2E-01), module preview (E2E-02), funding status (E2E-03), deploy progress (E2E-04)</what-built>
  <how-to-verify>
    1. Run full E2E suite: `cd /Users/theo/workspace_tokamak/trh-platform && npx playwright test --reporter=list`
    2. Verify all tests pass (expect 8+ test cases: 4 presets x E2E-01 + 4 presets x E2E-02 + E2E-03 unfunded + E2E-03 funded + E2E-04)
    3. Optionally run with headed mode to visually inspect: `npx playwright test --headed`
    4. Check HTML report: `npx playwright show-report`
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
1. `npx playwright test --reporter=list` runs all E2E tests successfully
2. 4 presets x wizard flow tests pass (E2E-01)
3. 4 presets x module preview tests pass (E2E-02)
4. Funding unfunded/funded scenarios pass (E2E-03)
5. Deploy initiation and navigation pass (E2E-04)
</verification>

<success_criteria>
- `npx playwright test` exits with code 0
- Test output shows >= 10 passing test cases
- All 4 requirement IDs (E2E-01~04) have corresponding test descriptions
- No test uses `page.waitForTimeout()` (anti-pattern)
</success_criteria>

<output>
After completion, create `.planning/phases/04-e2e-wizard-scenarios/04-02-SUMMARY.md`
</output>
