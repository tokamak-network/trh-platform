---
phase: 02-docker-stack-deploy-target
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/deploy-local.test.ts
  - tests/unit/deploy-aws.test.ts
autonomous: true
requirements: [DTGT-01, DTGT-02, DTGT-03, DTGT-04]

must_haves:
  truths:
    - "Local Docker 배포 경로의 docker compose 명령 시퀀스가 올바른 순서로 호출된다"
    - "AWS EC2 배포 경로의 terraform init/plan/apply 시퀀스가 올바른 순서로 호출된다"
    - "Local/AWS 공통 로직(compose 파일 존재 확인)과 인프라별 분기가 분리된다"
    - "Security Group ingress 포트가 22, 3000, 8000으로 설정되어 있다"
  artifacts:
    - path: "tests/unit/deploy-local.test.ts"
      provides: "Local Docker deploy sequence tests (DTGT-01, DTGT-03)"
      min_lines: 50
    - path: "tests/unit/deploy-aws.test.ts"
      provides: "AWS EC2 deploy sequence tests (DTGT-02, DTGT-03, DTGT-04)"
      min_lines: 60
  key_links:
    - from: "tests/unit/deploy-local.test.ts"
      to: "child_process"
      via: "vi.mock('child_process')"
      pattern: "vi\\.mock.*child_process"
    - from: "tests/unit/deploy-aws.test.ts"
      to: "ec2/main.tf"
      via: "fs.readFileSync for security group port extraction"
      pattern: "readFileSync.*main\\.tf"
---

<objective>
Local Docker와 AWS EC2 두 배포 경로의 명령 시퀀스를 child_process mock으로 검증하고, Terraform main.tf에서 Security Group 포트를 정규식으로 추출하여 검증하는 테스트를 구현한다.

Purpose: DTGT-01~04 요구사항을 충족하는 배포 경로 시퀀스 검증
Output: deploy-local.test.ts, deploy-aws.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-docker-stack-deploy-target/02-RESEARCH.md

<interfaces>
<!-- Deploy command sequences from RESEARCH.md -->

Local Docker sequence (from docker.ts + Makefile):
1. docker --version              (check installed)
2. docker info                   (check running)
3. docker compose -f <path> pull (pull images)
4. docker compose -f <path> up -d (start containers)
5. docker compose -f <path> ps   (health check poll)

AWS EC2 sequence (from Makefile ec2-deploy):
1. aws sts get-caller-identity   (verify credentials)
2. terraform -chdir=ec2 init     (initialize)
3. terraform -chdir=ec2 plan     (plan with variables)
4. terraform -chdir=ec2 apply -auto-approve (apply)
5. Remote provisioning via SSH   (cloud-init + install.sh + make setup)

Security Group ports: 22 (SSH), 3000 (Frontend), 8000 (Backend)

<!-- Phase 1 test patterns -->
From tests/unit/funding-flow.test.ts pattern:
- // @vitest-environment node
- describe/it BDD structure
- vi.mock for external dependencies
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Local Docker deploy sequence tests (DTGT-01, DTGT-03 local part)</name>
  <files>tests/unit/deploy-local.test.ts</files>
  <read_first>
    - src/main/docker.ts (Docker 명령 시퀀스 참조 — 직접 import하지 않고 명령 패턴만 참고)
    - Makefile (make up, make setup 명령 참조)
    - resources/docker-compose.yml (compose 파일 경로 확인)
    - tests/unit/funding-flow.test.ts (Phase 1 vi.mock 패턴 참조)
    - .planning/phases/02-docker-stack-deploy-target/02-RESEARCH.md (Pattern 2: child_process mock, Pitfall 2/3)
  </read_first>
  <behavior>
    - DTGT-01-a: Local 배포 시 docker --version이 먼저 호출되어 설치 확인
    - DTGT-01-b: docker info가 호출되어 데몬 실행 확인
    - DTGT-01-c: docker compose -f <composePath> pull이 호출되어 이미지 풀
    - DTGT-01-d: docker compose -f <composePath> up -d가 호출되어 컨테이너 시작
    - DTGT-01-e: 명령들이 올바른 순서로 호출됨 (version -> info -> pull -> up -d)
    - DTGT-03-local: Local 경로는 docker compose 명령을 사용하고 terraform 명령을 사용하지 않음
  </behavior>
  <action>
`tests/unit/deploy-local.test.ts` 생성 (per D-04, D-05):
- `// @vitest-environment node` 주석
- vi.mock('child_process') 사용 (per D-04)
- vi.mock 팩토리 내에서 exec와 spawn 모두 vi.fn()으로 mock (Pitfall 3 — inline 값만 사용)

**로컬 배포 시뮬레이션 함수 작성:**
docker.ts를 직접 import하지 않는다 (Pitfall 2 — Electron app 의존성). 대신 테스트 파일 내에 Local deploy 시퀀스를 순수 함수로 구현:
```typescript
async function localDeploySequence(composePath: string): Promise<void> {
  // 1. Check Docker installed
  await execPromise('docker --version');
  // 2. Check Docker running
  await execPromise('docker info');
  // 3. Pull images
  await execPromise(`docker compose -f ${composePath} pull`);
  // 4. Start containers
  await execPromise(`docker compose -f ${composePath} up -d`);
}
```
- execPromise는 child_process.exec를 Promise로 감싼 헬퍼

**테스트:**
- describe('Local Docker Deploy Sequence')
  - it('DTGT-01: calls docker commands in correct order')
    - localDeploySequence(composePath) 호출
    - exec mock의 호출 순서 검증: calls[0]에 'docker --version', calls[1]에 'docker info', calls[2]에 'docker compose ... pull', calls[3]에 'docker compose ... up -d'
  - it('DTGT-03: local path uses docker compose, not terraform')
    - 모든 exec 호출에 'terraform'이 포함되지 않음을 단언
    - 모든 exec 호출에 'docker'가 포함됨을 단언
  </action>
  <verify>
    <automated>cd /Users/theo/workspace_tokamak/trh-platform && npx vitest run tests/unit/deploy-local.test.ts 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - DTGT-01 테스트: docker compose 명령 시퀀스가 올바른 순서로 호출됨을 검증
    - DTGT-03 local part: local 경로가 docker compose만 사용하고 terraform을 사용하지 않음을 검증
    - docker.ts를 직접 import하지 않음 (Electron 의존성 회피)
    - vi.mock('child_process')로 exec/spawn 모킹
  </acceptance_criteria>
  <done>npx vitest run tests/unit/deploy-local.test.ts 실행 시 모든 테스트 PASS</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AWS EC2 deploy sequence + security group tests (DTGT-02, DTGT-03 AWS part, DTGT-04)</name>
  <files>tests/unit/deploy-aws.test.ts</files>
  <read_first>
    - ec2/main.tf (Terraform 설정, security group ingress rules 확인)
    - ec2/variables.tf (Terraform 변수 확인)
    - Makefile (ec2-deploy, ec2-setup 명령 참조)
    - .planning/phases/02-docker-stack-deploy-target/02-RESEARCH.md (Pattern 3: Terraform file text parsing, Pitfall 5)
  </read_first>
  <behavior>
    - DTGT-02-a: AWS 배포 시 aws sts get-caller-identity가 먼저 호출됨
    - DTGT-02-b: terraform -chdir=ec2 init -> plan -> apply -auto-approve 순서로 호출됨
    - DTGT-02-c: 명령들이 올바른 순서로 호출됨
    - DTGT-03-aws: AWS 경로는 terraform 명령을 사용하고 docker compose up을 직접 사용하지 않음
    - DTGT-04: ec2/main.tf의 security group에 ingress 포트 22, 3000, 8000이 정의되어 있음
  </behavior>
  <action>
`tests/unit/deploy-aws.test.ts` 생성 (per D-03, D-05):
- `// @vitest-environment node` 주석
- vi.mock('child_process') 사용 (per D-03)

**AWS 배포 시뮬레이션 함수:**
docker.ts를 직접 import하지 않는다. 테스트 내 순수 함수:
```typescript
async function awsDeploySequence(): Promise<void> {
  await execPromise('aws sts get-caller-identity');
  await execPromise('terraform -chdir=ec2 init');
  await execPromise('terraform -chdir=ec2 plan');
  await execPromise('terraform -chdir=ec2 apply -auto-approve');
}
```

**Security Group 포트 추출 함수:**
```typescript
function extractIngressPorts(tfContent: string): number[] {
  const ports: number[] = [];
  const regex = /ingress\s*\{[\s\S]*?from_port\s*=\s*(\d+)[\s\S]*?\}/g;
  let match;
  while ((match = regex.exec(tfContent)) !== null) {
    ports.push(parseInt(match[1], 10));
  }
  return ports;
}
```
- 정규식에 [\s\S]*? 사용으로 멀티라인 매칭 (Pitfall 5 대응)

**테스트:**
- describe('AWS EC2 Deploy Sequence')
  - it('DTGT-02: calls terraform commands in correct order')
    - awsDeploySequence() 호출
    - exec mock 호출 순서 검증: aws sts -> terraform init -> terraform plan -> terraform apply
  - it('DTGT-03: AWS path uses terraform, not docker compose up')
    - 모든 exec 호출에 'docker compose up'이 포함되지 않음
    - exec 호출에 'terraform'이 포함됨
  - it('DTGT-04: security group opens ports 22, 3000, 8000')
    - readFileSync로 ec2/main.tf 읽기
    - extractIngressPorts()로 포트 추출
    - 포트 22, 3000, 8000이 모두 존재
    - 총 3개 포트만 존재 (unexpected 포트 없음)
  </action>
  <verify>
    <automated>cd /Users/theo/workspace_tokamak/trh-platform && npx vitest run tests/unit/deploy-aws.test.ts 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - DTGT-02 테스트: terraform 명령 시퀀스가 올바른 순서로 호출됨을 검증
    - DTGT-03 AWS part: AWS 경로가 terraform을 사용하고 docker compose up을 직접 사용하지 않음을 검증
    - DTGT-04 테스트: ec2/main.tf에서 ingress 포트 22, 3000, 8000만 정의됨을 검증
    - HCL 파서 불필요 — 정규식으로 충분
  </acceptance_criteria>
  <done>npx vitest run tests/unit/deploy-aws.test.ts 실행 시 모든 테스트 PASS</done>
</task>

</tasks>

<verification>
```bash
# 전체 테스트 suite 확인 (Phase 1 + Phase 2 모두)
cd /Users/theo/workspace_tokamak/trh-platform && npx vitest run
```
</verification>

<success_criteria>
- deploy-local.test.ts의 DTGT-01, DTGT-03(local) 테스트가 모두 PASS
- deploy-aws.test.ts의 DTGT-02, DTGT-03(aws), DTGT-04 테스트가 모두 PASS
- child_process mock 패턴으로 실제 Docker/Terraform CLI 실행 없이 검증
- ec2/main.tf 정규식 파싱으로 security group 포트 검증
- 기존 Phase 1 테스트가 깨지지 않음
</success_criteria>

<output>
After completion, create `.planning/phases/02-docker-stack-deploy-target/02-02-SUMMARY.md`
</output>
