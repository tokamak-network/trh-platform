---
phase: 02-docker-stack-deploy-target
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/schemas/docker-compose.schema.ts
  - tests/helpers/load-compose.ts
  - tests/unit/docker-stack.test.ts
  - package.json
autonomous: true
requirements: [DOCK-01, DOCK-02, DOCK-03, DOCK-04]

must_haves:
  truths:
    - "resources/docker-compose.yml이 Zod 스키마 validation을 통과한다"
    - "postgres -> backend -> platform-ui 의존성 순서가 검증된다"
    - "postgres와 backend에 healthcheck이 정의되어 있음이 검증된다"
    - "서비스별 필수 환경변수가 정의되어 있음이 검증된다"
  artifacts:
    - path: "tests/schemas/docker-compose.schema.ts"
      provides: "Docker Compose Zod schema (ServiceSchema, DockerComposeSchema)"
      exports: ["DockerComposeSchema", "DockerCompose", "ServiceSchema"]
    - path: "tests/helpers/load-compose.ts"
      provides: "YAML parse + Zod validate helper"
      exports: ["loadCompose"]
    - path: "tests/unit/docker-stack.test.ts"
      provides: "DOCK-01~04 unit tests"
      min_lines: 40
  key_links:
    - from: "tests/unit/docker-stack.test.ts"
      to: "tests/helpers/load-compose.ts"
      via: "import loadCompose"
      pattern: "import.*loadCompose.*from.*load-compose"
    - from: "tests/helpers/load-compose.ts"
      to: "resources/docker-compose.yml"
      via: "fs.readFileSync + js-yaml parse"
      pattern: "yaml\\.load.*readFileSync"
---

<objective>
Docker compose 스키마 검증 테스트를 구현한다. js-yaml으로 resources/docker-compose.yml을 파싱하고 Zod 스키마로 services/volumes 구조, 의존성 순서, healthcheck, 환경변수를 검증하는 unit test를 작성한다.

Purpose: DOCK-01~04 요구사항을 충족하는 docker-compose.yml 구조 검증
Output: docker-compose.schema.ts, load-compose.ts, docker-stack.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-docker-stack-deploy-target/02-RESEARCH.md

<interfaces>
<!-- Phase 1 established patterns to follow -->

From tests/schemas/preset.schema.ts:
```typescript
import { z } from 'zod';
export const PresetDefinitionSchema = z.object({ ... });
export type PresetDefinition = z.infer<typeof PresetDefinitionSchema>;
export const PresetsFixtureSchema = z.record(...);
```

From tests/helpers/load-fixtures.ts:
```typescript
import { readFileSync } from 'fs';
import { join } from 'path';
import { PresetsFixtureSchema, type PresetsFixture } from '../schemas/preset.schema';
export function loadPresets(): PresetsFixture {
  const raw = JSON.parse(readFileSync(join(FIXTURES_DIR, 'presets.json'), 'utf-8'));
  return PresetsFixtureSchema.parse(raw);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install js-yaml, create Docker Compose Zod schema and YAML loader helper</name>
  <files>package.json, tests/schemas/docker-compose.schema.ts, tests/helpers/load-compose.ts</files>
  <read_first>
    - resources/docker-compose.yml (검증 대상 파일, 실제 구조 파악)
    - tests/schemas/preset.schema.ts (Phase 1 Zod 패턴 참조)
    - tests/helpers/load-fixtures.ts (Phase 1 헬퍼 패턴 참조)
    - .planning/phases/02-docker-stack-deploy-target/02-RESEARCH.md (스키마 구조 및 DOCK-04 해석)
  </read_first>
  <action>
1. `npm install --save-dev js-yaml @types/js-yaml` 실행 (per D-02)

2. `tests/schemas/docker-compose.schema.ts` 생성 (per D-01):
   - HealthcheckSchema: test (string[] | string), interval, timeout, retries, start_period (모두 optional)
   - DependsOnSchema: z.union — simple array form ["postgres"] + long form { postgres: { condition: "service_healthy" } }
   - ServiceSchema: image(string), container_name(optional), ports(string[] optional), environment(record|array optional), env_file(optional), depends_on(optional), volumes(string[] optional), healthcheck(optional), restart(optional)
   - VolumeSchema: z.union — null (named volume no config) | object with name/external/driver optional fields
   - DockerComposeSchema: version(optional), services(record of ServiceSchema), volumes(record of VolumeSchema optional), networks(optional)
   - Export: DockerComposeSchema, DockerCompose type, ServiceSchema, Service type

3. `tests/helpers/load-compose.ts` 생성:
   - js-yaml의 yaml.load()로 YAML 파싱 (safeLoad 아님, js-yaml 4.x에서는 load가 기본 safe)
   - 파싱 결과를 DockerComposeSchema.parse()로 검증
   - 기본 경로: resources/docker-compose.yml (join(__dirname, '..', '..', 'resources', 'docker-compose.yml'))
   - 선택적 filePath 파라미터로 다른 compose 파일도 로드 가능
   - Phase 1의 loadPresets() 패턴과 동일 구조
  </action>
  <verify>
    <automated>cd /Users/theo/workspace_tokamak/trh-platform && npx tsc --noEmit tests/schemas/docker-compose.schema.ts tests/helpers/load-compose.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - js-yaml과 @types/js-yaml이 package.json devDependencies에 추가됨
    - docker-compose.schema.ts가 DockerComposeSchema, ServiceSchema, 타입을 export
    - load-compose.ts가 loadCompose() 함수를 export하고 resources/docker-compose.yml을 파싱+검증
  </acceptance_criteria>
  <done>js-yaml 설치 완료, Zod 스키마와 YAML 로더 헬퍼가 타입 체크 통과</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Docker compose schema validation tests (DOCK-01~04)</name>
  <files>tests/unit/docker-stack.test.ts</files>
  <read_first>
    - tests/schemas/docker-compose.schema.ts (Task 1에서 생성한 스키마)
    - tests/helpers/load-compose.ts (Task 1에서 생성한 헬퍼)
    - resources/docker-compose.yml (검증 대상)
    - tests/unit/preset-config.test.ts (Phase 1 테스트 패턴 참조)
  </read_first>
  <behavior>
    - DOCK-01: loadCompose()로 resources/docker-compose.yml을 로드하면 Zod validation 통과, services에 postgres/backend/platform-ui 3개 서비스 존재, volumes에 named volumes 존재
    - DOCK-02: backend의 depends_on에 postgres가 있고 condition이 service_healthy, platform-ui의 depends_on에 backend가 있고 condition이 service_healthy
    - DOCK-03: postgres 서비스에 healthcheck 정의됨(test 필드에 pg_isready 포함), backend 서비스에 healthcheck 정의됨(test 필드에 curl 포함)
    - DOCK-04: postgres의 environment에 POSTGRES_USER/POSTGRES_PASSWORD/POSTGRES_DB 키 존재, backend의 environment에 PORT 키 존재, platform-ui의 environment에 NEXT_PUBLIC_API_BASE_URL 키 존재
  </behavior>
  <action>
`tests/unit/docker-stack.test.ts` 생성 (per D-05):
- `// @vitest-environment node` 주석 추가
- loadCompose()로 resources/docker-compose.yml 로드 (파일 상단에서 한 번만)
- describe('Docker Compose Schema (resources/docker-compose.yml)') 블록 내에:

  it('DOCK-01: valid services and volumes structure'):
    - Object.keys(compose.services)가 postgres, backend, platform-ui를 포함
    - compose.volumes가 정의됨
    - volumes 키에 trh_postgres_data 등 named volume 존재

  it('DOCK-02: dependency order postgres -> backend -> platform-ui'):
    - backend.depends_on이 record 형태이고 postgres 키가 존재하며 condition이 'service_healthy'
    - platform-ui.depends_on이 record 형태이고 backend 키가 존재하며 condition이 'service_healthy'

  it('DOCK-03: healthcheck defined for postgres and backend'):
    - postgres.healthcheck가 정의되고 test 필드에 'pg_isready' 문자열 포함
    - backend.healthcheck가 정의되고 test 필드에 'curl' 문자열 포함

  it('DOCK-04: required environment variables per service'):
    - postgres environment: POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB
    - backend environment: PORT (최소)
    - platform-ui environment: NEXT_PUBLIC_API_BASE_URL
    - environment가 record 형태인지 array 형태인지에 따라 검증 방법 분기 (resources 파일은 record 형태)
  </action>
  <verify>
    <automated>cd /Users/theo/workspace_tokamak/trh-platform && npx vitest run tests/unit/docker-stack.test.ts 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - 4개 테스트(DOCK-01~04)가 모두 통과
    - resources/docker-compose.yml의 실제 구조와 일치하는 검증
    - Phase 1과 동일한 describe/it BDD 구조
  </acceptance_criteria>
  <done>npx vitest run tests/unit/docker-stack.test.ts 실행 시 4개 테스트 모두 PASS</done>
</task>

</tasks>

<verification>
```bash
# 전체 테스트 suite 확인 (기존 Phase 1 테스트 포함)
cd /Users/theo/workspace_tokamak/trh-platform && npx vitest run
```
</verification>

<success_criteria>
- js-yaml이 devDependency로 설치됨
- docker-compose.schema.ts가 DockerComposeSchema를 export
- load-compose.ts가 resources/docker-compose.yml을 파싱+검증
- docker-stack.test.ts의 DOCK-01~04 테스트가 모두 PASS
- 기존 Phase 1 테스트가 깨지지 않음
</success_criteria>

<output>
After completion, create `.planning/phases/02-docker-stack-deploy-target/02-01-SUMMARY.md`
</output>
