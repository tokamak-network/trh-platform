---
phase: 03-ipc-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/schemas/ipc.schema.ts
  - tests/unit/ipc-channels.test.ts
  - tests/unit/ipc-payloads.test.ts
autonomous: true
requirements: [IPC-01, IPC-02, IPC-03]

must_haves:
  truths:
    - "preload.ts의 모든 invoke 채널이 index.ts+webview.ts의 handle 채널과 1:1 매칭됨을 자동 검증"
    - "webview-preload.ts의 invoke 채널이 대응하는 handle과 매칭됨을 자동 검증"
    - "keystore IPC 7개 채널의 payload 구조가 Zod 스키마와 일치"
    - "Docker IPC 18개 채널의 주요 payload 구조가 Zod 스키마와 일치"
  artifacts:
    - path: "tests/schemas/ipc.schema.ts"
      provides: "IPC payload Zod schemas for keystore and Docker channels"
      exports: ["KeystoreGetAddressesResponseSchema", "DockerStatusSchema", "PullProgressSchema", "PortCheckResultSchema", "BackendDependenciesSchema"]
    - path: "tests/unit/ipc-channels.test.ts"
      provides: "Channel registry matching test"
      contains: "extractChannels"
    - path: "tests/unit/ipc-payloads.test.ts"
      provides: "Keystore and Docker IPC payload validation tests"
      contains: "IPC-02"
  key_links:
    - from: "tests/unit/ipc-channels.test.ts"
      to: "src/main/preload.ts"
      via: "readFileSync + regex extraction"
      pattern: "ipcRenderer\\.invoke"
    - from: "tests/unit/ipc-channels.test.ts"
      to: "src/main/index.ts"
      via: "readFileSync + regex extraction"
      pattern: "ipcMain\\.handle"
    - from: "tests/unit/ipc-payloads.test.ts"
      to: "tests/schemas/ipc.schema.ts"
      via: "import schema, call .parse()"
      pattern: "import.*from.*ipc\\.schema"
---

<objective>
IPC 채널 레지스트리 일치 검증과 keystore/Docker payload Zod 스키마 검증 테스트를 작성한다.

Purpose: Electron main/renderer 간 IPC 채널명이 정확히 매칭되는지, payload 구조가 올바른지를 자동으로 검증하여 채널 불일치 버그를 사전 방지
Output: ipc.schema.ts (Zod schemas), ipc-channels.test.ts, ipc-payloads.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ipc-integration/03-CONTEXT.md
@.planning/phases/03-ipc-integration/03-RESEARCH.md

@tests/schemas/preset.schema.ts
@tests/unit/deploy-aws.test.ts
@vitest.config.mts

<interfaces>
<!-- Existing Zod schema pattern from Phase 1 -->
From tests/schemas/preset.schema.ts:
```typescript
import { z } from 'zod';
export const ChainDefaultsSchema = z.object({ ... });
export type ChainDefaults = z.infer<typeof ChainDefaultsSchema>;
```

<!-- Existing readFileSync + regex pattern from Phase 2 -->
From tests/unit/deploy-aws.test.ts:
```typescript
import { readFileSync } from 'fs';
import { join } from 'path';
const tfContent = readFileSync(tfPath, 'utf-8');
const regex = /ingress\s*\{[\s\S]*?from_port\s*=\s*(\d+)[\s\S]*?\}/g;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IPC payload Zod schemas 작성</name>
  <files>tests/schemas/ipc.schema.ts</files>
  <read_first>
    - tests/schemas/preset.schema.ts (Zod schema 패턴 참조)
    - src/main/preload.ts (DockerStatus, PullProgress, PortCheckResult, BackendDependencies 인터페이스 확인)
    - src/main/keystore.ts (KeyRole 타입, 함수 시그니처 확인)
  </read_first>
  <action>
    tests/schemas/ipc.schema.ts 파일을 새로 생성한다. Phase 1의 preset.schema.ts와 동일한 패턴으로 Zod 스키마를 정의한다 (per D-03).

    파일 상단에 `// @vitest-environment node` 주석을 추가한다.

    **Keystore IPC schemas (IPC-02):**
    - `KeyRoleEnum`: z.enum(['admin', 'proposer', 'batcher', 'challenger', 'sequencer'])
    - `KeystoreStoreInputSchema`: z.object({ seedPhrase: z.string() }) -- keystore:store의 입력
    - `KeystoreGetAddressesResponseSchema`: z.record(KeyRoleEnum, z.string().startsWith('0x')) -- keystore:get-addresses 응답
    - `KeystorePreviewAddressesInputSchema`: z.object({ seedPhrase: z.string() }) -- keystore:preview-addresses 입력
    - `KeystoreHasResponseSchema`: z.boolean() -- keystore:has 응답
    - `KeystoreIsAvailableResponseSchema`: z.boolean() -- keystore:is-available 응답
    - `KeystoreValidateInputSchema`: z.object({ mnemonic: z.string() }) -- keystore:validate 입력
    - `KeystoreValidateResponseSchema`: z.boolean() -- keystore:validate 응답

    **Docker IPC schemas (IPC-03):**
    - `DockerStatusSchema`: z.object({ installed: z.boolean(), running: z.boolean(), containersUp: z.boolean(), healthy: z.boolean(), error: z.string().optional() })
    - `PullProgressSchema`: z.object({ service: z.string(), progress: z.number(), status: z.string() })
    - `PortCheckResultSchema`: z.object({ port: z.number(), inUse: z.boolean(), process: z.string().optional() })
    - `BackendDependenciesSchema`: z.object({ missing: z.array(z.string()), installed: z.array(z.string()) })

    모든 스키마와 inferred 타입을 named export로 내보낸다.
  </action>
  <verify>
    <automated>cd /Users/theo/workspace_tokamak/trh-platform && npx tsx -e "import { KeystoreGetAddressesResponseSchema, DockerStatusSchema, PullProgressSchema, PortCheckResultSchema, BackendDependenciesSchema } from './tests/schemas/ipc.schema'; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/schemas/ipc.schema.ts 파일이 존재한다
    - 파일에 `export const KeyRoleEnum = z.enum` 문자열이 포함된다
    - 파일에 `export const KeystoreGetAddressesResponseSchema` 문자열이 포함된다
    - 파일에 `export const DockerStatusSchema` 문자열이 포함된다
    - 파일에 `export const PullProgressSchema` 문자열이 포함된다
    - 파일에 `export const PortCheckResultSchema` 문자열이 포함된다
    - 파일에 `export const BackendDependenciesSchema` 문자열이 포함된다
    - 파일 첫 줄이 `// @vitest-environment node`이다
  </acceptance_criteria>
  <done>Keystore 7개 + Docker 4개 주요 IPC payload에 대한 Zod 스키마가 정의되고 export됨</done>
</task>

<task type="auto">
  <name>Task 2: IPC 채널 레지스트리 매칭 테스트 (IPC-01)</name>
  <files>tests/unit/ipc-channels.test.ts</files>
  <read_first>
    - src/main/preload.ts (invoke 채널명 전체 확인)
    - src/main/webview-preload.ts (webview invoke 채널명 확인)
    - src/main/index.ts (handle 채널명 전체 확인)
    - src/main/webview.ts (registerWebviewIpcHandlers 내 handle 채널 확인)
    - tests/unit/deploy-aws.test.ts (readFileSync + regex 정적 파싱 패턴 참조)
  </read_first>
  <action>
    tests/unit/ipc-channels.test.ts 파일을 새로 생성한다. Phase 2의 deploy-aws.test.ts readFileSync+regex 정적 파싱 패턴을 재사용한다 (per D-01, D-02).

    파일 상단에 `// @vitest-environment node` 주석을 추가한다.

    **헬퍼 함수:**
    `extractChannels(filePath: string, regex: RegExp): Set<string>` — readFileSync로 파일을 읽고 정규식 매치를 Set으로 반환.

    **정규식 패턴:**
    - invoke 추출: `/ipcRenderer\.invoke\(['"]([^'"]+)['"]/g`
    - handle 추출: `/ipcMain\.handle\(['"]([^'"]+)['"]/g`
    - on 추출: `/ipcRenderer\.on\(['"]([^'"]+)['"]/g`

    **파일 경로:**
    - preloadPath: `join(__dirname, '..', '..', 'src', 'main', 'preload.ts')`
    - webviewPreloadPath: `join(__dirname, '..', '..', 'src', 'main', 'webview-preload.ts')`
    - indexPath: `join(__dirname, '..', '..', 'src', 'main', 'index.ts')`
    - webviewPath: `join(__dirname, '..', '..', 'src', 'main', 'webview.ts')`

    **테스트 케이스:**
    1. `IPC-01: all preload.ts invoke channels have matching handlers` — preload.ts에서 invoke 채널 추출, index.ts+webview.ts에서 handle 채널 추출, preload invoke가 모두 handle에 포함되는지 `expect(allHandleChannels.has(channel)).toBe(true)` 로 검증. 실패 시 어떤 채널이 누락인지 알 수 있도록 에러 메시지에 채널명 포함: `expect(allHandleChannels.has(channel), \`Missing handler for invoke channel: ${channel}\`).toBe(true)`

    2. `IPC-01: all webview-preload.ts invoke channels have matching handlers` — webview-preload.ts에서 invoke 채널 추출, 동일하게 handle 매칭 검증.

    3. `IPC-01: no orphan handlers without corresponding invoke` — handle 채널 중 어떤 invoke 파일에도 없는 채널이 없는지 검증. (preload + webview-preload invoke 합집합과 비교)

    4. `IPC-01: preload.ts invoke channel count is at least 50` — 추출된 invoke 채널 수가 예상 범위 내인지 sanity check. `expect(invokeChannels.size).toBeGreaterThanOrEqual(50)`

    5. `IPC-01: on channels are distinct from invoke channels` — preload.ts의 on 채널과 invoke 채널이 겹치지 않는지 검증.

    describe 블록: `describe('IPC Channel Registry (IPC-01)', () => { ... })`
  </action>
  <verify>
    <automated>cd /Users/theo/workspace_tokamak/trh-platform && npx vitest run tests/unit/ipc-channels.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/ipc-channels.test.ts 파일이 존재한다
    - 파일에 `extractChannels` 함수가 정의되어 있다
    - 파일에 `ipcRenderer\.invoke` 정규식 패턴이 포함된다
    - 파일에 `ipcMain\.handle` 정규식 패턴이 포함된다
    - 파일에 `preload.ts` 경로 참조가 포함된다
    - 파일에 `webview.ts` 경로 참조가 포함된다 (Pitfall 2 방지)
    - 파일에 `webview-preload.ts` 경로 참조가 포함된다 (Pitfall 3 방지)
    - `npx vitest run tests/unit/ipc-channels.test.ts`가 0 exit code로 완료된다
    - 최소 5개 테스트 케이스가 통과한다
  </acceptance_criteria>
  <done>preload.ts/webview-preload.ts의 모든 invoke 채널이 index.ts+webview.ts의 handle과 1:1 매칭됨이 자동 검증됨</done>
</task>

<task type="auto">
  <name>Task 3: IPC payload 검증 테스트 (IPC-02, IPC-03)</name>
  <files>tests/unit/ipc-payloads.test.ts</files>
  <read_first>
    - tests/schemas/ipc.schema.ts (Task 1에서 생성한 스키마)
    - src/main/preload.ts (DockerStatus, PullProgress 등 인터페이스 정의 확인)
    - src/main/keystore.ts (getAddresses, previewAddresses 등 반환 타입 확인)
    - src/main/docker.ts (getDockerStatus 등 반환 타입 확인)
  </read_first>
  <action>
    tests/unit/ipc-payloads.test.ts 파일을 새로 생성한다 (per D-09). Zod 스키마로 payload 구조를 검증한다 (per D-03).

    파일 상단에 `// @vitest-environment node` 주석을 추가한다.

    **접근법:** preload.ts에 정의된 TypeScript 인터페이스(DockerStatus, PullProgress 등)를 readFileSync로 읽어 필드명을 정규식으로 추출하고, Task 1에서 정의한 Zod 스키마의 키와 비교한다. 이를 통해 preload.ts 인터페이스와 Zod 스키마 간 구조적 일치를 검증한다.

    **Keystore payload 테스트 (IPC-02):**
    1. `IPC-02: keystore:get-addresses response matches schema` — mock 응답 데이터 `{ admin: '0xabc...', proposer: '0xdef...', batcher: '0x123...', challenger: '0x456...', sequencer: '0x789...' }` 를 KeystoreGetAddressesResponseSchema.parse()로 검증
    2. `IPC-02: keystore:get-addresses rejects invalid role names` — `{ admin: '0xabc', invalidRole: '0xdef' }` 같은 잘못된 role이 포함되면 parse 실패 검증
    3. `IPC-02: keystore:has response matches schema` — `true` 값을 KeystoreHasResponseSchema.parse()로 검증
    4. `IPC-02: keystore:validate response matches schema` — `false` 값을 KeystoreValidateResponseSchema.parse()로 검증

    **Docker payload 테스트 (IPC-03):**
    5. `IPC-03: docker:get-status response matches schema` — mock 데이터 `{ installed: true, running: true, containersUp: true, healthy: true }` 를 DockerStatusSchema.parse()로 검증
    6. `IPC-03: docker:get-status with error matches schema` — `{ installed: true, running: false, containersUp: false, healthy: false, error: 'Docker daemon not running' }` 검증
    7. `IPC-03: pull-progress event matches schema` — `{ service: 'trh-backend', progress: 45, status: 'downloading' }` 를 PullProgressSchema.parse()로 검증
    8. `IPC-03: port check result matches schema` — `{ port: 3000, inUse: true, process: 'node' }` 를 PortCheckResultSchema.parse()로 검증
    9. `IPC-03: backend dependencies result matches schema` — `{ missing: ['trh-sdk'], installed: ['docker'] }` 를 BackendDependenciesSchema.parse()로 검증

    **Structural alignment 테스트:**
    10. `IPC-02: preload.ts DockerStatus interface fields align with DockerStatusSchema` — preload.ts를 readFileSync로 읽어 `interface DockerStatus` 블록에서 필드명을 추출하고, DockerStatusSchema.shape의 키 목록과 비교.

    describe 블록 두 개: `describe('Keystore IPC Payloads (IPC-02)', () => { ... })`, `describe('Docker IPC Payloads (IPC-03)', () => { ... })`
  </action>
  <verify>
    <automated>cd /Users/theo/workspace_tokamak/trh-platform && npx vitest run tests/unit/ipc-payloads.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/ipc-payloads.test.ts 파일이 존재한다
    - 파일에 `import.*from.*ipc.schema` import 문이 있다
    - 파일에 `KeystoreGetAddressesResponseSchema.parse` 호출이 있다
    - 파일에 `DockerStatusSchema.parse` 호출이 있다
    - 파일에 `PullProgressSchema.parse` 호출이 있다
    - 파일에 `IPC-02` 문자열이 테스트 이름에 포함된다
    - 파일에 `IPC-03` 문자열이 테스트 이름에 포함된다
    - `npx vitest run tests/unit/ipc-payloads.test.ts`가 0 exit code로 완료된다
    - 최소 9개 테스트 케이스가 통과한다
  </acceptance_criteria>
  <done>Keystore 7개 채널 + Docker 18개 채널의 주요 payload 구조가 Zod 스키마 기반으로 검증됨</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/unit/ipc-channels.test.ts` -- 모든 채널 매칭 테스트 통과
2. `npx vitest run tests/unit/ipc-payloads.test.ts` -- 모든 payload 스키마 테스트 통과
3. `npx vitest run tests/unit/` -- 기존 Phase 1/2 테스트 포함 전체 통과 (regression 없음)
</verification>

<success_criteria>
- IPC 채널 레지스트리 테스트가 preload.ts(50 invoke) + webview-preload.ts(8 invoke) 전체를 커버
- handle 측 검증이 index.ts(42) + webview.ts(8) 두 파일 모두 포함
- Keystore/Docker payload Zod 검증이 각각 최소 4개, 5개 테스트 케이스로 커버
- 기존 Phase 1/2 테스트가 깨지지 않음
</success_criteria>

<output>
After completion, create `.planning/phases/03-ipc-integration/03-01-SUMMARY.md`
</output>
