---
phase: 03-ipc-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/schemas/api-contract.schema.ts
  - tests/schemas/webview.schema.ts
  - tests/unit/webview-injection.test.ts
autonomous: true
requirements: [IPC-04, IPC-05]

must_haves:
  truths:
    - "POST /stacks/thanos/preset-deploy 요청 스키마가 Go 구조체 PresetDeployRequest와 일치"
    - "window.__TRH_DESKTOP_ACCOUNTS__ payload가 5개 role(admin/proposer/batcher/challenger/sequencer) x {address, privateKey} 구조"
    - "window.__TRH_AWS_CREDENTIALS__ payload가 {accessKeyId, secretAccessKey, sessionToken?, source} 구조"
    - "webview.ts의 injection 코드가 스키마에 정의된 필드를 실제로 사용함을 정적 분석으로 검증"
  artifacts:
    - path: "tests/schemas/api-contract.schema.ts"
      provides: "Backend API contract Zod schemas"
      exports: ["PresetDeployRequestSchema", "DeploymentNetworkEnum"]
    - path: "tests/schemas/webview.schema.ts"
      provides: "WebView injection payload Zod schemas"
      exports: ["DesktopAccountsSchema", "AwsCredentialsSchema"]
    - path: "tests/unit/webview-injection.test.ts"
      provides: "Backend API contract + WebView injection tests"
      contains: "IPC-04"
  key_links:
    - from: "tests/unit/webview-injection.test.ts"
      to: "src/main/webview.ts"
      via: "readFileSync + regex for injection payload field extraction"
      pattern: "__TRH_DESKTOP_ACCOUNTS__"
    - from: "tests/schemas/api-contract.schema.ts"
      to: "trh-backend Go struct"
      via: "Zod schema mirrors PresetDeployRequest fields"
      pattern: "presetId.*chainName.*network.*seedPhrase"
---

<objective>
Backend API contract(POST /preset-deploy)과 WebView credential injection payload 구조를 Zod 스키마로 정의하고 검증하는 테스트를 작성한다.

Purpose: Backend API 요청 스키마와 WebView 전역변수 injection의 payload 구조가 올바른지 자동 검증하여 contract 불일치 방지
Output: api-contract.schema.ts, webview.schema.ts, webview-injection.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ipc-integration/03-CONTEXT.md
@.planning/phases/03-ipc-integration/03-RESEARCH.md

@tests/schemas/preset.schema.ts
@vitest.config.mts

<interfaces>
<!-- Existing Zod schema pattern from Phase 1 -->
From tests/schemas/preset.schema.ts:
```typescript
import { z } from 'zod';
export const ChainDefaultsSchema = z.object({ ... });
export type ChainDefaults = z.infer<typeof ChainDefaultsSchema>;
```

<!-- Backend API contract from research (Go struct -> TypeScript Zod) -->
From trh-backend/pkg/api/dtos/thanos.go PresetDeployRequest:
```go
type PresetDeployRequest struct {
  PresetID        string                    `json:"presetId" binding:"required"`
  ChainName       string                    `json:"chainName" binding:"required"`
  Network         entities.DeploymentNetwork `json:"network" binding:"required"`
  SeedPhrase      string                    `json:"seedPhrase" binding:"required"`
  InfraProvider   string                    `json:"infraProvider" binding:"required"`
  AwsAccessKey    string                    `json:"awsAccessKey"`
  AwsSecretKey    string                    `json:"awsSecretKey"`
  AwsRegion       string                    `json:"awsRegion"`
  L1RpcUrl        string                    `json:"l1RpcUrl" binding:"required"`
  L1BeaconUrl     string                    `json:"l1BeaconUrl" binding:"required"`
  FeeToken        string                    `json:"feeToken"`
  ReuseDeployment *bool                     `json:"reuseDeployment"`
  Overrides       []Override                `json:"overrides"`
}
// DeploymentNetwork values: "Mainnet", "Testnet", "LocalDevnet"
```

<!-- WebView injection payload structures from research -->
From src/main/webview.ts:
```typescript
// window.__TRH_DESKTOP_ACCOUNTS__ (5 roles)
{ admin: { address, privateKey }, proposer: { address, privateKey }, ... }

// window.__TRH_AWS_CREDENTIALS__
{ accessKeyId, secretAccessKey, sessionToken?, source }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend API contract + WebView injection Zod schemas 작성</name>
  <files>tests/schemas/api-contract.schema.ts, tests/schemas/webview.schema.ts</files>
  <read_first>
    - tests/schemas/preset.schema.ts (Zod 스키마 패턴 참조)
    - src/main/webview.ts (injectKeystoreAccounts, injectAwsCredentials 함수에서 payload 구조 확인)
  </read_first>
  <action>
    두 개의 Zod 스키마 파일을 생성한다 (per D-05, D-07).

    **tests/schemas/api-contract.schema.ts:**
    파일 상단에 `// @vitest-environment node` 주석.

    ```typescript
    import { z } from 'zod';

    export const DeploymentNetworkEnum = z.enum(['Mainnet', 'Testnet', 'LocalDevnet']);

    export const OverrideSchema = z.object({
      field: z.string(),
      value: z.any(),
    });

    export const PresetDeployRequestSchema = z.object({
      presetId: z.string(),
      chainName: z.string(),
      network: DeploymentNetworkEnum,
      seedPhrase: z.string(),
      infraProvider: z.enum(['aws', 'local']),
      awsAccessKey: z.string().optional(),
      awsSecretKey: z.string().optional(),
      awsRegion: z.string().optional(),
      l1RpcUrl: z.string(),
      l1BeaconUrl: z.string(),
      feeToken: z.string().optional(),
      reuseDeployment: z.boolean().optional(),
      overrides: z.array(OverrideSchema).optional(),
    });

    export type PresetDeployRequest = z.infer<typeof PresetDeployRequestSchema>;
    export type DeploymentNetwork = z.infer<typeof DeploymentNetworkEnum>;
    ```

    Go의 `binding:"required"` 태그가 있는 필드는 z.string()으로 (required), 없는 필드는 z.string().optional()로 정의한다 (Pitfall 5 주의).

    **tests/schemas/webview.schema.ts:**
    파일 상단에 `// @vitest-environment node` 주석.

    ```typescript
    import { z } from 'zod';

    const AccountEntrySchema = z.object({
      address: z.string().startsWith('0x'),
      privateKey: z.string().startsWith('0x'),
    });

    export const DesktopAccountsSchema = z.object({
      admin: AccountEntrySchema,
      proposer: AccountEntrySchema,
      batcher: AccountEntrySchema,
      challenger: AccountEntrySchema,
      sequencer: AccountEntrySchema,
    });

    export const AwsCredentialsSchema = z.object({
      accessKeyId: z.string().min(1),
      secretAccessKey: z.string().min(1),
      sessionToken: z.string().optional(),
      source: z.string().min(1),
    });

    export type DesktopAccounts = z.infer<typeof DesktopAccountsSchema>;
    export type AwsCredentials = z.infer<typeof AwsCredentialsSchema>;
    ```
  </action>
  <verify>
    <automated>cd /Users/theo/workspace_tokamak/trh-platform && npx tsx -e "import { PresetDeployRequestSchema } from './tests/schemas/api-contract.schema'; import { DesktopAccountsSchema, AwsCredentialsSchema } from './tests/schemas/webview.schema'; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/schemas/api-contract.schema.ts 파일이 존재한다
    - 파일에 `export const PresetDeployRequestSchema` 문자열이 포함된다
    - 파일에 `export const DeploymentNetworkEnum = z.enum(['Mainnet', 'Testnet', 'LocalDevnet'])` 문자열이 포함된다
    - 파일에 `presetId: z.string()` (required, not optional) 문자열이 포함된다
    - 파일에 `awsAccessKey: z.string().optional()` 문자열이 포함된다
    - tests/schemas/webview.schema.ts 파일이 존재한다
    - 파일에 `export const DesktopAccountsSchema` 문자열이 포함된다
    - 파일에 `export const AwsCredentialsSchema` 문자열이 포함된다
    - webview.schema.ts에 5개 role(admin, proposer, batcher, challenger, sequencer) 모두 포함된다
  </acceptance_criteria>
  <done>Backend API contract(PresetDeployRequest)와 WebView injection payload(DesktopAccounts, AwsCredentials) Zod 스키마가 정의되고 export됨</done>
</task>

<task type="auto">
  <name>Task 2: Backend API contract + WebView injection 테스트 (IPC-04, IPC-05)</name>
  <files>tests/unit/webview-injection.test.ts</files>
  <read_first>
    - tests/schemas/api-contract.schema.ts (Task 1에서 생성한 API contract 스키마)
    - tests/schemas/webview.schema.ts (Task 1에서 생성한 WebView 스키마)
    - src/main/webview.ts (injectKeystoreAccounts, injectAwsCredentials 함수 — payload 생성 로직, executeJavaScript 호출 패턴 확인)
  </read_first>
  <action>
    tests/unit/webview-injection.test.ts 파일을 새로 생성한다 (per D-09).

    파일 상단에 `// @vitest-environment node` 주석을 추가한다.

    **Backend API contract 테스트 (IPC-04):**
    describe('Backend API Contract (IPC-04)', () => { ... })

    1. `IPC-04: valid preset-deploy request passes schema` — 완전한 mock 요청:
       ```
       { presetId: 'general', chainName: 'my-l2', network: 'Testnet', seedPhrase: 'test test test test test test test test test test test junk', infraProvider: 'local', l1RpcUrl: 'http://localhost:8545', l1BeaconUrl: 'http://localhost:5052' }
       ```
       를 PresetDeployRequestSchema.parse()로 검증.

    2. `IPC-04: preset-deploy request with AWS fields passes schema` — AWS 배포용 mock 요청:
       ```
       { presetId: 'defi', chainName: 'defi-chain', network: 'Mainnet', seedPhrase: '...', infraProvider: 'aws', awsAccessKey: 'AKIAIOSFODNN7EXAMPLE', awsSecretKey: 'wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY', awsRegion: 'ap-northeast-2', l1RpcUrl: 'https://mainnet.infura.io/v3/...', l1BeaconUrl: 'https://beacon.example.com', feeToken: 'TON', reuseDeployment: false, overrides: [{ field: 'challengePeriod', value: 7200 }] }
       ```
       를 parse()로 검증.

    3. `IPC-04: preset-deploy request rejects missing required fields` — `{ presetId: 'general' }` (나머지 required 필드 누락)가 parse 시 ZodError를 throw하는지 검증. `expect(() => PresetDeployRequestSchema.parse(...)).toThrow()`

    4. `IPC-04: preset-deploy request rejects invalid network value` — `network: 'InvalidNet'`이 parse 실패하는지 검증.

    5. `IPC-04: preset-deploy request rejects invalid infraProvider` — `infraProvider: 'gcp'`가 parse 실패하는지 검증.

    **WebView injection 테스트 (IPC-05):**
    describe('WebView Injection (IPC-05)', () => { ... })

    6. `IPC-05: valid desktop accounts payload passes schema` — 5개 role 전체가 포함된 mock payload를 DesktopAccountsSchema.parse()로 검증:
       ```
       { admin: { address: '0x1234...', privateKey: '0xabcd...' }, proposer: { address: '0x2345...', privateKey: '0xbcde...' }, batcher: { address: '0x3456...', privateKey: '0xcdef...' }, challenger: { address: '0x4567...', privateKey: '0xdef0...' }, sequencer: { address: '0x5678...', privateKey: '0xef01...' } }
       ```

    7. `IPC-05: desktop accounts rejects missing role` — sequencer 누락 시 parse 실패 검증.

    8. `IPC-05: desktop accounts rejects non-0x address` — `address: 'invalid'` (0x 접두사 없음) 시 parse 실패 검증.

    9. `IPC-05: valid AWS credentials payload passes schema` — `{ accessKeyId: 'AKIAIOSFODNN7EXAMPLE', secretAccessKey: 'wJalrXUtnFEMI/...', source: 'sso' }` 를 AwsCredentialsSchema.parse()로 검증.

    10. `IPC-05: AWS credentials with sessionToken passes schema` — sessionToken 포함된 payload 검증.

    11. `IPC-05: AWS credentials rejects empty accessKeyId` — `{ accessKeyId: '', ... }` 가 parse 실패 검증.

    **정적 분석 테스트 (IPC-05 보강, per D-08):**
    12. `IPC-05: webview.ts contains __TRH_DESKTOP_ACCOUNTS__ injection` — webview.ts를 readFileSync로 읽어 `__TRH_DESKTOP_ACCOUNTS__` 문자열이 존재하는지 검증.

    13. `IPC-05: webview.ts contains __TRH_AWS_CREDENTIALS__ injection` — 동일하게 `__TRH_AWS_CREDENTIALS__` 문자열 존재 검증.

    14. `IPC-05: webview.ts injection covers all 5 account roles` — webview.ts 소스에서 `admin`, `proposer`, `batcher`, `challenger`, `sequencer` 5개 role 문자열이 모두 존재하는지 검증.

    15. `IPC-05: webview.ts injection covers AWS credential fields` — webview.ts 소스에서 `accessKeyId`, `secretAccessKey`, `sessionToken`, `source` 4개 필드가 모두 존재하는지 검증.
  </action>
  <verify>
    <automated>cd /Users/theo/workspace_tokamak/trh-platform && npx vitest run tests/unit/webview-injection.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/webview-injection.test.ts 파일이 존재한다
    - 파일에 `import.*from.*api-contract.schema` import 문이 있다
    - 파일에 `import.*from.*webview.schema` import 문이 있다
    - 파일에 `PresetDeployRequestSchema.parse` 호출이 있다
    - 파일에 `DesktopAccountsSchema.parse` 호출이 있다
    - 파일에 `AwsCredentialsSchema.parse` 호출이 있다
    - 파일에 `IPC-04` 문자열이 테스트 이름에 포함된다
    - 파일에 `IPC-05` 문자열이 테스트 이름에 포함된다
    - 파일에 `__TRH_DESKTOP_ACCOUNTS__` 문자열이 포함된다
    - 파일에 `__TRH_AWS_CREDENTIALS__` 문자열이 포함된다
    - 파일에 `readFileSync` 호출이 있다 (정적 분석 테스트용)
    - `npx vitest run tests/unit/webview-injection.test.ts`가 0 exit code로 완료된다
    - 최소 14개 테스트 케이스가 통과한다
  </acceptance_criteria>
  <done>Backend API contract(POST /preset-deploy) 요청 스키마와 WebView injection(accounts+credentials) payload 구조가 Zod 기반으로 검증되고, webview.ts 정적 분석으로 필드 존재가 확인됨</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/unit/webview-injection.test.ts` -- 모든 API contract + WebView injection 테스트 통과
2. `npx vitest run tests/unit/` -- 기존 Phase 1/2 테스트 포함 전체 통과 (regression 없음)
</verification>

<success_criteria>
- PresetDeployRequest 스키마가 Go 구조체의 required/optional 필드를 정확히 반영
- DesktopAccounts 스키마가 5개 role x {address, privateKey} 구조를 검증
- AwsCredentials 스키마가 {accessKeyId, secretAccessKey, sessionToken?, source} 구조를 검증
- webview.ts 정적 분석으로 injection 코드가 올바른 필드를 사용함을 확인
- 기존 Phase 1/2 테스트가 깨지지 않음
</success_criteria>

<output>
After completion, create `.planning/phases/03-ipc-integration/03-02-SUMMARY.md`
</output>
