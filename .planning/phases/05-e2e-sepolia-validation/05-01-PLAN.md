---
phase: 05-e2e-sepolia-validation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/e2e/helpers/matrix-config.ts
  - tests/e2e/helpers/stack-resolver.ts
  - tests/e2e/matrix/crosstrade-health.live.spec.ts
  - tests/unit/crosstrade-e2e-helpers.test.ts
autonomous: true
requirements: [E2E-01, E2E-02, E2E-03]

must_haves:
  truths:
    - "isModuleEnabled('defi', 'crossTrade') returns true"
    - "StackUrls includes crossTradeUrl field with localhost:3004 default"
    - "crosstrade-health.live.spec.ts exists with 3 test cases covering E2E-01, E2E-02, E2E-03"
    - "Unit tests cover matrix-config crossTrade presence and stack-resolver crossTradeUrl"
  artifacts:
    - path: "tests/e2e/helpers/matrix-config.ts"
      provides: "DeFi preset includes crossTrade module"
      contains: "crossTrade"
    - path: "tests/e2e/helpers/stack-resolver.ts"
      provides: "crossTradeUrl field in StackUrls"
      contains: "crossTradeUrl"
    - path: "tests/e2e/matrix/crosstrade-health.live.spec.ts"
      provides: "Live E2E spec for CrossTrade health"
      min_lines: 80
    - path: "tests/unit/crosstrade-e2e-helpers.test.ts"
      provides: "Unit tests for helper changes"
      min_lines: 30
  key_links:
    - from: "tests/e2e/matrix/crosstrade-health.live.spec.ts"
      to: "tests/e2e/helpers/matrix-config.ts"
      via: "isModuleEnabled(config.preset, 'crossTrade')"
      pattern: "isModuleEnabled.*crossTrade"
    - from: "tests/e2e/matrix/crosstrade-health.live.spec.ts"
      to: "tests/e2e/helpers/stack-resolver.ts"
      via: "resolveStackUrls returns crossTradeUrl"
      pattern: "urls\\.crossTradeUrl"
---

<objective>
Fix matrix-config.ts DeFi crossTrade bug, extend stack-resolver.ts with crossTradeUrl, write crosstrade-health.live.spec.ts (RED phase), and add unit tests for helper changes.

Purpose: TDD Red phase -- all tests written and failing (live spec fails because no deployed Sepolia stack; unit tests verify helper correctness).
Output: 4 files modified/created, test infrastructure ready for GREEN verification in Plan 05-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-e2e-sepolia-validation/05-RESEARCH.md

@tests/e2e/matrix/drb-health.live.spec.ts
@tests/e2e/helpers/matrix-config.ts
@tests/e2e/helpers/stack-resolver.ts
@tests/e2e/helpers/poll.ts
@tests/e2e/helpers/health-checks.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From tests/e2e/helpers/matrix-config.ts:
```typescript
export type Preset = 'general' | 'defi' | 'gaming' | 'full';
export type ModuleName = 'bridge' | 'blockExplorer' | 'monitoring' | 'uptimeService' | 'crossTrade' | 'drb';
export function isModuleEnabled(preset: Preset, module: ModuleName): boolean;
export function getStackConfig(): StackConfig;
```

From tests/e2e/helpers/stack-resolver.ts:
```typescript
export interface StackUrls {
  stackId: string; l2Rpc: string; l2ChainId: number;
  bridgeUrl: string; explorerUrl: string; explorerApiUrl: string;
  grafanaUrl: string; prometheusUrl: string; uptimeUrl: string;
  drbUrl: string; bundlerUrl: string;
  // MISSING: crossTradeUrl -- must be added
}
export async function loginBackend(backendUrl?: string): Promise<string>;
export async function resolveStackUrls(chainName: string, token?: string): Promise<StackUrls>;
```

From tests/e2e/helpers/poll.ts:
```typescript
export async function pollUntil<T>(fn: () => Promise<T | null>, label: string, timeoutMs?: number, intervalMs?: number): Promise<T>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix matrix-config.ts + Extend stack-resolver.ts + Unit Tests</name>
  <files>tests/e2e/helpers/matrix-config.ts, tests/e2e/helpers/stack-resolver.ts, tests/unit/crosstrade-e2e-helpers.test.ts</files>
  <behavior>
    - Test 1: isModuleEnabled('defi', 'crossTrade') === true (currently false -- this is the bug)
    - Test 2: isModuleEnabled('gaming', 'crossTrade') === true (already true)
    - Test 3: isModuleEnabled('full', 'crossTrade') === true (already true)
    - Test 4: isModuleEnabled('general', 'crossTrade') === false (general has no crossTrade)
    - Test 5: StackUrls type has crossTradeUrl property (TypeScript compile check)
    - Test 6: resolveStackUrls returns crossTradeUrl with default 'http://localhost:3004' when metadata has no crossTradeUrl
  </behavior>
  <action>
1. Create `tests/unit/crosstrade-e2e-helpers.test.ts` with Vitest tests:
   - Import `isModuleEnabled, PRESET_MODULES` from `../e2e/helpers/matrix-config`
   - Test that `isModuleEnabled('defi', 'crossTrade')` returns `true`
   - Test that `isModuleEnabled('general', 'crossTrade')` returns `false`
   - Test that `PRESET_MODULES.defi` includes `'crossTrade'`
   - Test that `PRESET_MODULES.gaming` includes `'crossTrade'`
   - Test that `PRESET_MODULES.full` includes `'crossTrade'`
   - Test that `PRESET_MODULES.general` does NOT include `'crossTrade'`

2. Run tests -- the `defi` test MUST fail (RED phase confirms the bug).

3. Fix `tests/e2e/helpers/matrix-config.ts` line 36:
   BEFORE: `defi: ['bridge', 'blockExplorer', 'monitoring', 'uptimeService'],`
   AFTER:  `defi: ['bridge', 'blockExplorer', 'monitoring', 'uptimeService', 'crossTrade'],`

4. Run tests -- all unit tests MUST pass (GREEN).

5. Extend `tests/e2e/helpers/stack-resolver.ts`:
   - Add `crossTradeUrl: string;` to `StackUrls` interface (after `bundlerUrl`)
   - Add `crossTradeUrl: 'http://localhost:3004',` to `LOCAL_DEFAULTS`
   - Add `crossTradeUrl: (meta.crossTradeUrl as string) || LOCAL_DEFAULTS.crossTradeUrl,` to the return object in `resolveStackUrls()`

6. Commit: `fix(05-01): add crossTrade to DeFi PRESET_MODULES + extend StackUrls with crossTradeUrl`
  </action>
  <verify>
    <automated>cd /Users/theo/workspace_tokamak/trh-platform && npx vitest run tests/unit/crosstrade-e2e-helpers.test.ts</automated>
  </verify>
  <done>
    - isModuleEnabled('defi', 'crossTrade') returns true
    - StackUrls.crossTradeUrl exists with 'http://localhost:3004' default
    - All unit tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Write crosstrade-health.live.spec.ts (RED -- no live stack)</name>
  <files>tests/e2e/matrix/crosstrade-health.live.spec.ts</files>
  <action>
Create `tests/e2e/matrix/crosstrade-health.live.spec.ts` following the `drb-health.live.spec.ts` pattern exactly.

File structure:

```typescript
/**
 * Matrix Health Check -- CrossTrade (Tier 2)
 *
 * SKIPS for General preset (crossTrade not in module list).
 * Verifies CrossTrade L2 contract deployment, L1 setChainInfo registration,
 * and dApp accessibility.
 *
 * Usage:
 *   LIVE_PRESET=defi LIVE_FEE_TOKEN=USDT LIVE_CHAIN_NAME=<chain> npx playwright test \
 *     --config playwright.live.config.ts tests/e2e/matrix/crosstrade-health.live.spec.ts
 */

import { test, expect } from '@playwright/test';
import { getStackConfig, isModuleEnabled } from '../helpers/matrix-config';
import { resolveStackUrls, loginBackend, StackUrls } from '../helpers/stack-resolver';
import { pollUntil } from '../helpers/poll';
import { ethers } from 'ethers';

const config = getStackConfig();
let urls: StackUrls;
let jwt: string;
let crossTradeIntegration: Record<string, unknown>;

test.describe(`CrossTrade Health [${config.preset}/${config.feeToken}]`, () => {
  test.describe.configure({ mode: 'serial' });

  test.beforeAll(async () => {
    test.skip(!isModuleEnabled(config.preset, 'crossTrade'), 'CrossTrade not in preset');
    urls = await resolveStackUrls(config.chainName);
    jwt = await loginBackend();

    // Fetch CrossTrade integration from backend API
    const backendUrl = process.env.LIVE_BACKEND_URL ?? 'http://localhost:8000';
    const resp = await fetch(
      `${backendUrl}/api/v1/stacks/thanos/${urls.stackId}/integrations`,
      { headers: { Authorization: `Bearer ${jwt}` } }
    );
    expect(resp.ok, `Failed to fetch integrations: ${resp.status}`).toBe(true);

    const body = await resp.json() as Record<string, unknown>;
    const data = (body.data ?? body) as Record<string, unknown>;
    const integrations = (data.integrations ?? []) as Record<string, unknown>[];
    const found = integrations.find((i) => i.type === 'cross-trade');
    expect(found, 'CrossTrade integration not found in backend').toBeDefined();
    crossTradeIntegration = found!;
    expect(crossTradeIntegration.status, 'CrossTrade integration must be installed').toBe('installed');
  });

  // E2E-01: 4 L2 CrossTrade contracts deployed
  test('L2 CrossTrade contracts deployed (4 addresses)', async () => {
    const meta = crossTradeIntegration.metadata as Record<string, unknown>;
    const contracts = meta?.contracts as Record<string, string>;
    expect(contracts, 'No contracts in integration metadata').toBeDefined();

    const requiredContracts = [
      'L2CrossTrade',
      'L2CrossTradeProxy',
      'L2toL2CrossTradeL2',
      'L2toL2CrossTradeProxy',
    ];

    for (const name of requiredContracts) {
      expect(contracts[name], `Missing contract address for ${name}`).toBeTruthy();
    }

    // Verify each address has deployed bytecode on L2
    const provider = new ethers.JsonRpcProvider(urls.l2Rpc);
    for (const name of requiredContracts) {
      const addr = contracts[name];
      const code = await provider.getCode(addr);
      expect(code, `No bytecode at ${name} (${addr})`).not.toBe('0x');
      expect(code.length, `Bytecode too short at ${name}`).toBeGreaterThan(10);
    }
  });

  // E2E-02: L1 setChainInfo registered
  test('L1 setChainInfo registered (tx hashes exist)', async () => {
    const meta = crossTradeIntegration.metadata as Record<string, unknown>;

    expect(
      meta.l1_registration_tx_hash,
      'L1CrossTradeProxy setChainInfo tx hash required'
    ).toBeTruthy();

    expect(
      meta.l1_l2l2_tx_hash,
      'L2toL2CrossTradeL1 setChainInfo tx hash required'
    ).toBeTruthy();

    // Optional: verify tx receipts on Sepolia L1
    const sepoliaRpc = process.env.SEPOLIA_RPC_URL;
    if (sepoliaRpc) {
      const l1Provider = new ethers.JsonRpcProvider(sepoliaRpc);
      const receipt1 = await l1Provider.getTransactionReceipt(
        meta.l1_registration_tx_hash as string
      );
      expect(receipt1, 'L1 registration tx not found on Sepolia').not.toBeNull();
      expect(receipt1!.status, 'L1 registration tx failed').toBe(1);

      const receipt2 = await l1Provider.getTransactionReceipt(
        meta.l1_l2l2_tx_hash as string
      );
      expect(receipt2, 'L1 L2toL2 tx not found on Sepolia').not.toBeNull();
      expect(receipt2!.status, 'L1 L2toL2 tx failed').toBe(1);
    }
  });

  // E2E-03: CrossTrade dApp accessible
  test('CrossTrade dApp accessible at expected URL', async () => {
    const dappUrl = urls.crossTradeUrl ?? 'http://localhost:3004';

    const reachable = await pollUntil(
      async () => {
        try {
          const r = await fetch(dappUrl, { signal: AbortSignal.timeout(5000) });
          return r.status < 500 ? true : null;
        } catch {
          return null;
        }
      },
      `CrossTrade dApp at ${dappUrl}`,
      60_000,
      5_000
    );
    expect(reachable, `CrossTrade dApp not reachable at ${dappUrl}`).toBe(true);
  });
});
```

Key points:
- Follow the `drb-health.live.spec.ts` pattern exactly (serial mode, beforeAll with skip check)
- Use `isModuleEnabled(config.preset, 'crossTrade')` for preset gating
- Fetch integration metadata from backend API in beforeAll
- E2E-01: Check 4 contract addresses exist in metadata AND have bytecode on L2
- E2E-02: Check l1_registration_tx_hash and l1_l2l2_tx_hash exist; optionally verify on Sepolia
- E2E-03: Use pollUntil for dApp HTTP reachability (60s timeout, 5s interval)
- Use urls.crossTradeUrl from the extended StackUrls

Commit: `test(05-01): add crosstrade-health.live.spec.ts for E2E Sepolia validation`
  </action>
  <verify>
    <automated>cd /Users/theo/workspace_tokamak/trh-platform && npx tsc --noEmit tests/e2e/matrix/crosstrade-health.live.spec.ts 2>&1 || echo "TypeScript check (may need playwright types)" && ls -la tests/e2e/matrix/crosstrade-health.live.spec.ts</automated>
  </verify>
  <done>
    - crosstrade-health.live.spec.ts exists with 3 test cases
    - File imports from helpers correctly (matrix-config, stack-resolver, poll)
    - Covers E2E-01 (4 contract addresses), E2E-02 (tx hashes), E2E-03 (dApp HTTP)
    - File compiles without TypeScript errors
  </done>
</task>

</tasks>

<verification>
1. Unit tests pass: `npx vitest run tests/unit/crosstrade-e2e-helpers.test.ts`
2. Live spec file exists and is syntactically correct
3. `isModuleEnabled('defi', 'crossTrade')` returns `true`
4. StackUrls type includes `crossTradeUrl`
</verification>

<success_criteria>
- matrix-config.ts PRESET_MODULES.defi includes 'crossTrade'
- stack-resolver.ts StackUrls has crossTradeUrl with 'http://localhost:3004' default
- crosstrade-health.live.spec.ts has 3 test cases covering E2E-01, E2E-02, E2E-03
- Unit tests for helper changes pass
- All files compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-e2e-sepolia-validation/05-01-SUMMARY.md`
</output>
