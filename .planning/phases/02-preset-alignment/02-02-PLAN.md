---
phase: 02-preset-alignment
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - ../trh-sdk/pkg/constants/chain.go
  - ../trh-backend/pkg/services/thanos/presets/service.go
  - ../trh-backend/pkg/services/thanos/stack_lifecycle.go
autonomous: true
requirements: [SDK-08, SDK-09, SDK-10, BE-01, BE-02]

must_haves:
  truths:
    - "SDK PresetModules에서 DeFi preset에 crossTrade=true가 존재한다"
    - "SDK PresetModules에서 Gaming preset에 crossTrade 키가 없다"
    - "SDK PresetModules에서 Full preset에 crossTrade=true가 유지된다"
    - "Backend DeFi preset의 Modules[crossTrade]=true이고 HelmValues[crossTrade.enabled]=true이다"
    - "Backend Gaming preset의 Modules[crossTrade]=false이고 HelmValues[crossTrade.enabled]=false이다"
    - "Backend localUnsupported에 crossTrade가 없어서 로컬 배포가 허용된다"
    - "DeFi/Full 로컬 배포 시 CrossTrade integration entity가 생성된다"
  artifacts:
    - path: "../trh-sdk/pkg/constants/chain.go"
      provides: "SDK preset module definitions with correct crossTrade alignment"
      contains: "crossTrade"
    - path: "../trh-backend/pkg/services/thanos/presets/service.go"
      provides: "Backend preset definitions with correct crossTrade values"
      contains: "crossTrade"
    - path: "../trh-backend/pkg/services/thanos/stack_lifecycle.go"
      provides: "Local deployment without crossTrade blocking"
  key_links:
    - from: "../trh-sdk/pkg/constants/chain.go"
      to: "../trh-sdk/pkg/stacks/thanos/deploy_chain.go"
      via: "PresetModules map lookup for crossTrade"
      pattern: "presetModules\\[.crossTrade.\\]"
    - from: "../trh-backend/pkg/services/thanos/presets/service.go"
      to: "../trh-backend/pkg/services/thanos/stack_lifecycle.go"
      via: "def.Modules iteration in CreateThanosStack"
      pattern: "def\\.Modules"
    - from: "../trh-backend/pkg/services/thanos/stack_lifecycle.go"
      to: "integration entity creation loop"
      via: "localUnsupported removal unblocks crossTrade entity"
      pattern: "IntegrationTypeCrossTrade"
---

<objective>
SDK PresetModules와 Backend preset definitions에서 DeFi=crossTrade true, Gaming=crossTrade false 정합성을 구현하고, Backend localUnsupported에서 crossTrade를 제거하여 로컬 배포를 허용한다.

Purpose: Phase 2의 모든 요구사항(SDK-08~10, BE-01~02)을 달성하는 실제 코드 변경. Plan 01에서 작성한 테스트가 GREEN으로 전환된다.
Output: 3개 파일 수정 (chain.go, service.go, stack_lifecycle.go)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-preset-alignment/02-RESEARCH.md
@.planning/phases/02-preset-alignment/02-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs from the codebase -->

From ../trh-sdk/pkg/constants/chain.go (line 18-45):
```go
var PresetModules = map[string]map[string]bool{
    PresetGeneral: { "bridge": true, "blockExplorer": true },
    PresetDeFi: { "bridge": true, "blockExplorer": true, "monitoring": true, "uptimeService": true },
    PresetGaming: { "bridge": true, "blockExplorer": true, "monitoring": true, "crossTrade": true, "uptimeService": true, "drb": true },
    PresetFull: { "bridge": true, "blockExplorer": true, "monitoring": true, "crossTrade": true, "uptimeService": true, "drb": true },
}
```

From ../trh-backend/pkg/services/thanos/presets/service.go (line 95-171):
- defi.Modules["crossTrade"] = false (line 103)
- defi.HelmValues["crossTrade.enabled"] = false (line 123)
- gaming.Modules["crossTrade"] = true (line 141)
- gaming.HelmValues["crossTrade.enabled"] = true (line 162)

From ../trh-backend/pkg/services/thanos/stack_lifecycle.go (line 122-133):
```go
localUnsupported := map[string]bool{
    "crossTrade": true,
}
isLocal := request.InfraProvider == "local"
// ...
if isLocal && localUnsupported[module] {
    continue
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SDK PresetModules crossTrade 정합성 수정 (SDK-08, SDK-09, SDK-10)</name>
  <files>../trh-sdk/pkg/constants/chain.go</files>
  <read_first>
    - ../trh-sdk/pkg/constants/chain.go (line 18-45: PresetModules map 전체)
    - ../trh-sdk/pkg/constants/chain_test.go (Plan 01에서 생성한 테스트, 목표 상태 확인)
  </read_first>
  <action>
    `../trh-sdk/pkg/constants/chain.go`의 PresetModules map을 수정한다:

    **1. PresetDeFi (line 23-28):** `"crossTrade": true,` 추가 (SDK-08)
    목표 상태:
    ```go
    PresetDeFi: {
        "bridge":        true,
        "blockExplorer": true,
        "monitoring":    true,
        "uptimeService": true,
        "crossTrade":    true,
    },
    ```

    **2. PresetGaming (line 29-36):** `"crossTrade": true,` 행 삭제 (SDK-09)
    목표 상태:
    ```go
    PresetGaming: {
        "bridge":        true,
        "blockExplorer": true,
        "monitoring":    true,
        "uptimeService": true,
        "drb":           true,
    },
    ```

    **3. PresetFull (line 37-44):** 변경 없음 — `"crossTrade": true` 유지 확인 (SDK-10)

    수정 후 go vet과 테스트 실행으로 검증.
  </action>
  <verify>
    <automated>cd ../trh-sdk && go vet ./pkg/constants/... && go test ./pkg/constants/... -run TestPresetModulesCrossTrade -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"crossTrade".*true' ../trh-sdk/pkg/constants/chain.go`에서 PresetDeFi 블록과 PresetFull 블록에만 crossTrade가 존재한다
    - `grep -A8 'PresetGaming' ../trh-sdk/pkg/constants/chain.go | grep -c 'crossTrade'` 결과가 0이다
    - `grep -A8 'PresetDeFi' ../trh-sdk/pkg/constants/chain.go | grep -c 'crossTrade'` 결과가 1이다
    - `cd ../trh-sdk && go test ./pkg/constants/... -run TestPresetModulesCrossTrade -v` 모든 subtest PASS
  </acceptance_criteria>
  <done>PresetDeFi에 crossTrade=true 추가, PresetGaming에서 crossTrade 제거, PresetFull은 유지. 4개 subtest 모두 PASS (TDD GREEN)</done>
</task>

<task type="auto">
  <name>Task 2: Backend preset definitions + localUnsupported 수정 (BE-01, BE-02)</name>
  <files>../trh-backend/pkg/services/thanos/presets/service.go, ../trh-backend/pkg/services/thanos/stack_lifecycle.go</files>
  <read_first>
    - ../trh-backend/pkg/services/thanos/presets/service.go (line 95-171: defi와 gaming 정의 전체)
    - ../trh-backend/pkg/services/thanos/stack_lifecycle.go (line 110-150: localUnsupported 맵, isLocal 변수, integration entity 루프)
    - ../trh-backend/pkg/services/thanos/stack_lifecycle_test.go (Plan 01에서 생성한 테스트)
  </read_first>
  <action>
    **파일 1: `../trh-backend/pkg/services/thanos/presets/service.go`**

    DeFi preset (line 95-131):
    - line 103: `"crossTrade": false,` -> `"crossTrade": true,`
    - line 123: `"crossTrade.enabled": false,` -> `"crossTrade.enabled": true,`

    Gaming preset (line 133-171):
    - line 141: `"crossTrade": true,` -> `"crossTrade": false,`
    - line 162: `"crossTrade.enabled": true,` -> `"crossTrade.enabled": false,`

    general과 full은 변경하지 않는다 (이미 올바름).

    **파일 2: `../trh-backend/pkg/services/thanos/stack_lifecycle.go`**

    BE-01: localUnsupported 맵과 관련 dead code 제거.

    line 122-125의 `localUnsupported` 선언 삭제:
    ```go
    // 삭제: localUnsupported := map[string]bool{
    // 삭제:     "crossTrade": true,
    // 삭제: }
    ```

    line 126의 `isLocal` 선언 삭제:
    ```go
    // 삭제: isLocal := request.InfraProvider == "local"
    ```

    line 131-133의 `if isLocal && localUnsupported[module]` 블록 삭제:
    ```go
    // 삭제: if isLocal && localUnsupported[module] {
    // 삭제:     continue
    // 삭제: }
    ```

    결과: `for module, enabled := range def.Modules` 루프에서 bridge skip과 !enabled skip만 남기고, localUnsupported 관련 코드는 모두 제거. crossTrade module이 enabled=true이면 항상 integration entity가 생성된다.

    수정 후 go vet과 테스트 실행으로 검증.
  </action>
  <verify>
    <automated>cd ../trh-backend && go vet ./pkg/services/thanos/... && go test ./pkg/services/thanos/... -run "TestLocalDeployment|TestLocalUnsupported" -v && go test ./pkg/services/thanos/presets/... -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'localUnsupported' ../trh-backend/pkg/services/thanos/stack_lifecycle.go` 결과가 0이다
    - `grep -c 'isLocal' ../trh-backend/pkg/services/thanos/stack_lifecycle.go` 결과가 0이다
    - `grep -A2 '"defi"' ../trh-backend/pkg/services/thanos/presets/service.go | head -5` 후 `grep '"crossTrade":.*true'`가 존재한다 (직접 확인 어려우면 line 103에 true 확인)
    - `cd ../trh-backend && go test ./pkg/services/thanos/... -run TestLocalDeploymentCrossTradeEntityCreation -v` 모든 subtest PASS
    - `cd ../trh-backend && go test ./pkg/services/thanos/presets/... -v` 기존 테스트도 PASS
  </acceptance_criteria>
  <done>Backend DeFi crossTrade=true, Gaming crossTrade=false, localUnsupported 제거 완료. 모든 테스트 PASS (TDD GREEN). BE-01과 BE-02 달성</done>
</task>

</tasks>

<verification>
Phase 2 전체 검증:
- `cd ../trh-sdk && go test ./pkg/constants/... -v` 모든 테스트 PASS
- `cd ../trh-backend && go test ./pkg/services/thanos/... -v` 모든 테스트 PASS
- `cd ../trh-backend && go test ./pkg/services/thanos/presets/... -v` 기존 preset 테스트 PASS
- SDK: DeFi에 crossTrade=true, Gaming에 crossTrade 없음, Full에 crossTrade=true
- Backend: DeFi crossTrade=true, Gaming crossTrade=false, localUnsupported 제거
</verification>

<success_criteria>
- SDK PresetModules: DeFi=crossTrade true, Gaming=crossTrade 없음, Full=crossTrade true (SDK-08, SDK-09, SDK-10)
- Backend presets: DeFi Modules+HelmValues crossTrade=true, Gaming Modules+HelmValues crossTrade=false (BE-02 전제)
- Backend stack_lifecycle: localUnsupported 맵과 isLocal 변수 및 관련 if 블록 제거 (BE-01)
- 모든 기존 테스트 + 새 테스트 PASS (TDD GREEN)
</success_criteria>

<output>
After completion, create `.planning/phases/02-preset-alignment/02-02-SUMMARY.md`
</output>
