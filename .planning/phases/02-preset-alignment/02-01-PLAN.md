---
phase: 02-preset-alignment
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - ../trh-sdk/pkg/constants/chain_test.go
  - ../trh-backend/pkg/services/thanos/stack_lifecycle_test.go
autonomous: true
requirements: [SDK-08, SDK-09, SDK-10, BE-01, BE-02]

must_haves:
  truths:
    - "SDK PresetModules에서 DeFi preset에 crossTrade=true가 존재한다"
    - "SDK PresetModules에서 Gaming preset에 crossTrade 키가 없다"
    - "SDK PresetModules에서 Full preset에 crossTrade=true가 유지된다"
    - "Backend localUnsupported에 crossTrade가 없어서 로컬 배포가 허용된다"
    - "DeFi/Full 로컬 배포 시 CrossTrade integration entity가 생성된다"
  artifacts:
    - path: "../trh-sdk/pkg/constants/chain_test.go"
      provides: "SDK preset alignment tests"
      contains: "TestPresetModulesCrossTrade"
    - path: "../trh-backend/pkg/services/thanos/stack_lifecycle_test.go"
      provides: "Backend local deployment crossTrade unblock tests"
      contains: "TestLocalDeploymentCrossTrade"
  key_links:
    - from: "../trh-sdk/pkg/constants/chain_test.go"
      to: "../trh-sdk/pkg/constants/chain.go"
      via: "import constants package, read PresetModules map"
      pattern: "constants\\.PresetModules"
    - from: "../trh-backend/pkg/services/thanos/stack_lifecycle_test.go"
      to: "../trh-backend/pkg/services/thanos/stack_lifecycle.go"
      via: "test verifies localUnsupported behavior indirectly"
      pattern: "crossTrade"
---

<objective>
Wave 0 테스트 스캐폴드: SDK PresetModules crossTrade 정합성 테스트와 Backend localUnsupported 제거 검증 테스트를 작성한다.

Purpose: TDD RED 단계 — 현재 코드에서 실패하는 테스트를 먼저 작성하여, Plan 02에서 코드 변경 시 GREEN으로 전환됨을 확인한다.
Output: 두 개의 테스트 파일 (chain_test.go, stack_lifecycle_test.go)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-preset-alignment/02-RESEARCH.md
@.planning/phases/02-preset-alignment/02-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SDK PresetModules crossTrade 정합성 테스트 작성</name>
  <files>../trh-sdk/pkg/constants/chain_test.go</files>
  <read_first>
    - ../trh-sdk/pkg/constants/chain.go (현재 PresetModules 정의 확인, line 18-45)
    - ../trh-sdk/go.mod (Go 버전 및 모듈 경로 확인)
  </read_first>
  <behavior>
    - Test: PresetModules["defi"]["crossTrade"] == true (SDK-08)
    - Test: PresetModules["gaming"]["crossTrade"] does not exist (_, ok := ...; ok == false) (SDK-09)
    - Test: PresetModules["full"]["crossTrade"] == true (SDK-10)
    - Test: PresetModules["general"]에는 crossTrade 키가 없다 (기존 동작 보호)
  </behavior>
  <action>
    새 파일 `../trh-sdk/pkg/constants/chain_test.go`를 생성한다. package는 `constants`로 선언한다 (같은 패키지 내부 테스트).

    테스트 함수 `TestPresetModulesCrossTrade(t *testing.T)`:
    - subtests 4개를 `t.Run`으로 작성:
      1. `"DeFi_has_crossTrade_true"`: `val, ok := PresetModules[PresetDeFi]["crossTrade"]`; `ok == true && val == true` assert
      2. `"Gaming_has_no_crossTrade"`: `_, ok := PresetModules[PresetGaming]["crossTrade"]`; `ok == false` assert
      3. `"Full_has_crossTrade_true"`: `val, ok := PresetModules[PresetFull]["crossTrade"]`; `ok == true && val == true` assert
      4. `"General_has_no_crossTrade"`: `_, ok := PresetModules[PresetGeneral]["crossTrade"]`; `ok == false` assert

    assert는 표준 `testing` 패키지만 사용 (외부 의존성 금지):
    ```go
    if !ok {
        t.Errorf("PresetModules[%q] should have crossTrade key", PresetDeFi)
    }
    if !val {
        t.Errorf("PresetModules[%q][crossTrade] = %v, want true", PresetDeFi, val)
    }
    ```

    이 테스트는 현재 코드에서 **DeFi subtest가 FAIL** (crossTrade 키 없음), **Gaming subtest가 FAIL** (crossTrade=true 존재)해야 한다. Full과 General은 PASS.
  </action>
  <verify>
    <automated>cd ../trh-sdk && go test ./pkg/constants/... -run TestPresetModulesCrossTrade -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ../trh-sdk/pkg/constants/chain_test.go 파일이 존재한다
    - `grep -c "TestPresetModulesCrossTrade" ../trh-sdk/pkg/constants/chain_test.go` 결과가 1 이상이다
    - `grep -c "t.Run" ../trh-sdk/pkg/constants/chain_test.go` 결과가 4이다 (4개 subtest)
    - `grep "PresetDeFi" ../trh-sdk/pkg/constants/chain_test.go` 결과가 있다
    - `grep "PresetGaming" ../trh-sdk/pkg/constants/chain_test.go` 결과가 있다
    - `cd ../trh-sdk && go vet ./pkg/constants/...` 가 에러 없이 통과한다
    - `cd ../trh-sdk && go test ./pkg/constants/... -run TestPresetModulesCrossTrade -v` 실행 시 DeFi_has_crossTrade_true와 Gaming_has_no_crossTrade subtest가 FAIL한다 (RED 상태)
  </acceptance_criteria>
  <done>chain_test.go에 TestPresetModulesCrossTrade 함수가 4개 subtest로 존재하고, go vet 통과하며, DeFi/Gaming subtest가 기대대로 FAIL한다 (TDD RED)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend stack_lifecycle crossTrade 로컬 배포 허용 테스트 작성</name>
  <files>../trh-backend/pkg/services/thanos/stack_lifecycle_test.go</files>
  <read_first>
    - ../trh-backend/pkg/services/thanos/stack_lifecycle.go (line 110-160: localUnsupported 로직, moduleToType 맵, integration entity 생성 루프)
    - ../trh-backend/pkg/services/thanos/presets/service.go (line 54-105: DefaultPresetDefinitions defi/gaming 구조)
    - ../trh-backend/go.mod (Go 버전 및 모듈 경로 확인)
    - ../trh-backend/pkg/services/thanos/presets/service_test.go (기존 테스트 패턴 참조)
  </read_first>
  <behavior>
    - Test: DeFi preset + local infra에서 crossTrade integration entity가 생성된다 (BE-01 + BE-02)
    - Test: Full preset + local infra에서 crossTrade integration entity가 생성된다 (BE-02)
    - Test: Gaming preset + local infra에서 crossTrade integration entity가 생성되지 않는다 (Gaming=false이므로)
  </behavior>
  <action>
    **주의:** stack_lifecycle.go의 integration entity 생성 로직은 `CreateThanosStack` 메서드 내부에 있어 직접 단위 테스트가 어렵다 (DB, 여러 서비스 의존성). 따라서 두 가지 레벨의 테스트를 작성한다:

    **접근 방식:** `stack_lifecycle.go`의 localUnsupported 맵과 관련 로직을 직접 테스트하기 어려우므로, preset definitions 레벨에서 검증하는 테스트를 작성한다. 이 테스트는 `presets` 패키지의 `DefaultPresetDefinitions`를 읽어 crossTrade 모듈 값이 올바른지 확인한다.

    새 파일 `../trh-backend/pkg/services/thanos/stack_lifecycle_test.go`를 생성한다. package는 `thanos_test`로 선언한다 (외부 패키지 테스트).

    테스트 함수 `TestLocalDeploymentCrossTradeEntityCreation(t *testing.T)`:

    이 함수는 stack_lifecycle.go의 integration entity 생성 루프를 **시뮬레이션**한다. 실제 DB 없이 로직만 재현:

    ```go
    func TestLocalDeploymentCrossTradeEntityCreation(t *testing.T) {
        // Simulate the localUnsupported map from stack_lifecycle.go
        // After BE-01, crossTrade should NOT be in localUnsupported
        localUnsupported := map[string]bool{
            // crossTrade should not be here after fix
        }

        testCases := []struct {
            name              string
            preset            string
            expectCrossTrade  bool
        }{
            {"DeFi_local_creates_crossTrade_entity", "defi", true},
            {"Full_local_creates_crossTrade_entity", "full", true},
            {"Gaming_local_no_crossTrade_entity", "gaming", false},
            {"General_local_no_crossTrade_entity", "general", false},
        }

        for _, tc := range testCases {
            t.Run(tc.name, func(t *testing.T) {
                def := presets.DefaultPresetDefinitions[tc.preset]
                crossTradeEnabled := def.Modules["crossTrade"]
                isLocal := true
                blocked := isLocal && localUnsupported["crossTrade"]

                wouldCreate := crossTradeEnabled && !blocked
                if wouldCreate != tc.expectCrossTrade {
                    t.Errorf("preset %q: crossTrade entity creation = %v, want %v (enabled=%v, blocked=%v)",
                        tc.preset, wouldCreate, tc.expectCrossTrade, crossTradeEnabled, blocked)
                }
            })
        }
    }
    ```

    import 필요: `"testing"`, presets 패키지의 full import path (go.mod에서 module path 확인하여 `{module}/pkg/services/thanos/presets` 형태).

    추가 테스트 함수 `TestLocalUnsupportedNoCrossTrade(t *testing.T)`:
    - stack_lifecycle.go 소스를 직접 읽어 `localUnsupported` 맵에 `crossTrade`가 없음을 확인하는 접근은 불가능하므로, preset level에서 DeFi가 crossTrade=true이고 localUnsupported에 의해 차단되지 않는 것을 검증하는 통합적 접근을 사용한다.
    - 이 테스트는 `presets.DefaultPresetDefinitions["defi"].Modules["crossTrade"]`가 `true`인지 확인한다 (BE-01의 전제조건인 preset 수정 검증).

    **현재 코드에서 이 테스트는 FAIL해야 한다:** DeFi의 `Modules["crossTrade"]`가 현재 `false`이므로 DeFi subtest가 실패한다.
  </action>
  <verify>
    <automated>cd ../trh-backend && go test ./pkg/services/thanos/... -run "TestLocalDeployment|TestLocalUnsupported" -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ../trh-backend/pkg/services/thanos/stack_lifecycle_test.go 파일이 존재한다
    - `grep -c "TestLocalDeploymentCrossTradeEntityCreation" ../trh-backend/pkg/services/thanos/stack_lifecycle_test.go` 결과가 1 이상이다
    - `grep "presets.DefaultPresetDefinitions" ../trh-backend/pkg/services/thanos/stack_lifecycle_test.go` 결과가 있다
    - `cd ../trh-backend && go vet ./pkg/services/thanos/...` 가 에러 없이 통과한다
    - `cd ../trh-backend && go test ./pkg/services/thanos/... -run TestLocalDeploymentCrossTradeEntityCreation -v` 실행 시 DeFi subtest가 FAIL한다 (현재 DeFi crossTrade=false이므로)
  </acceptance_criteria>
  <done>stack_lifecycle_test.go에 TestLocalDeploymentCrossTradeEntityCreation 함수가 존재하고, go vet 통과하며, DeFi subtest가 기대대로 FAIL한다 (TDD RED)</done>
</task>

</tasks>

<verification>
- `cd ../trh-sdk && go vet ./pkg/constants/...` 통과
- `cd ../trh-backend && go vet ./pkg/services/thanos/...` 통과
- 두 테스트 모두 RED 상태 (의도된 FAIL)
</verification>

<success_criteria>
- chain_test.go와 stack_lifecycle_test.go가 생성됨
- 두 파일 모두 go vet 통과
- 현재 코드 상태에서 DeFi/Gaming 관련 subtest가 FAIL (TDD RED 확인)
- Plan 02에서 코드 변경 후 모든 테스트가 PASS될 수 있는 구조
</success_criteria>

<output>
After completion, create `.planning/phases/02-preset-alignment/02-01-SUMMARY.md`
</output>
